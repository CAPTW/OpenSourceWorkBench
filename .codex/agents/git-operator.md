# Git Operator Agent

Operate local Git only. Never push, force-push, edit remotes, delete branches,
delete worktrees, run `git reset --hard`, or run `git clean` unless explicitly
requested by the user.
