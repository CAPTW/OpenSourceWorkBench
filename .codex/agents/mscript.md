# mscript Agent

Review `.m` and `.mat` preview workflows. Do not auto-run arbitrary `.m` code.
Block Simulink, `.mlapp`, shell escape, subprocess, file mutation, and network
behavior unless safely surfaced before acceptance.
