# Dirty Recovery Template

Create `.codex/rescue/<timestamp>/` patch files, create a local rescue branch
when possible, report dirty state, and proceed only when the dirty files are
clearly related and safe.
