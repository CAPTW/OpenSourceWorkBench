# Risk Register

| Risk | Impact | Mitigation | Fallback |
| --- | --- | --- | --- |
| Scope drift toward proprietary clone behavior | Confuses project identity and invites impossible expectations | Keep non-goals in README, North Star, guardrails, and review protocol | Remove or rewrite claims; park the feature behind a decision log |
| CAD import expectations | Pulls v0.1 into licensed, unstable, unsupported native formats | Accept only standard/exported STEP, IGES, STL, mesh, and similar formats | Document manual export workflow from commercial tools |
| External solver execution | Unsafe process control, unclear logs, and difficult failure recovery | Keep GUI to preparation, preview, validation, and result import; require future runner design | Provide case preparation and run instructions outside GUI |
| `.m` execution safety | Arbitrary scripts may mutate files, call shell commands, or depend on proprietary toolboxes | Preview `.m` and `.mat` content first; do not auto-run arbitrary `.m` code | Treat workflow as figure/data preview only until a sandboxed runner exists |
| GUI dependency burden | PySide6 can slow setup and block headless contributors | Keep GUI as an optional extra and keep CLI/unit tests GUI-free; OSW-AUTO-052 local UAT skipped live GUI interaction because PySide6 was missing and verified the CLI diagnostic instead | Ship CLI and docs workflows while GUI extras are unavailable |
| Validation without golden evidence | Weak research credibility and over-trust in demo outputs | Add validation matrix rows, assumptions, limitations, and golden fixtures as demos mature | Mark workflow as preview-only until evidence exists |
| Simulink, `.slx`, or `.mlapp` expectations | Creates MATLAB product compatibility burden | Limit script support to preview-first `.m` and `.mat` workflows | Defer as post-v0.1 research topic with explicit non-goal note |
| Full OpenFOAM UI or solver coverage | Expands beyond a bounded educational demo | Keep only cavity/duct templates and documented limitations | Ship template preparation/reporting without broad case editor |
| Industrial certification claims | Legal and trust risk | Ban certification/compliance/production accuracy language in docs and UI | Replace with educational/research validation wording |
| Heavy dependencies blocking bootstrap | Slows contributors and CI | Keep PySide6, mesh, viz, thermo, and script stacks as optional extras | Allow doctor to report missing optional modules without failing |
| Solver runtime artifacts committed | Pollutes repo and hides reproducibility issues | Use `.gitignore`, preflight checks, and review gates | Remove artifacts from index and store curated fixtures under examples/tests |
| Report output overclaiming results | Users may trust unvalidated results | Reports must list assumptions, limitations, and validation status | Block release of report feature until warnings are present |
| Plugin API too broad too early | Locks weak abstractions into v0.1 | Start with narrow importer/solver/report contracts | Keep experimental plugin APIs internal until stable |
| Optional solver tools missing locally | Demos fail on contributor machines | Doctor command reports availability; tests stay network-free and solver-free; OSW-AUTO-052 classified missing optional solver stacks as non-blocking P2 environment follow-ups | Provide fixture-only or template-only demo mode |
| Nonlinear contact/plasticity pressure | Pulls v0.1 into advanced nonlinear CAE validation | Mark as future research unless a later prompt explicitly defers it | Keep CalculiX v0.1 demo linear static only |
| License metadata drift after finalization | Future edits could make `LICENSE`, pyproject metadata, README, release checklist, or plugin metadata disagree | Keep `GPL-3.0-or-later` recorded in the decision log, license/version plan, release checklist, and release metadata QA check | Block release/tag prompts until metadata is realigned |
| RC local tag gate bypass | A local RC tag could be created before QA, notices, or final checklist evidence are reviewed | Require OSW-AUTO-043 feature-branch QA, squash merge, post-merge pre-tag QA, annotated tag verification, and clean `develop` state | Do not push or announce; if an unpushed local tag is rejected later, use maintainer-approved local tag deletion only |
| Release metadata tag-mode mismatch | A QA checker can reject valid post-tag evidence if it only supports pre-tag metadata prompts | Keep `check_release_metadata.py` split between strict pre-tag mode and RC-aware expected annotated tag mode | Block push gates until the checker accepts only the intended local RC tag and still rejects final or unexpected tags |
| Stale RC tag after release-gate follow-up commit | A later QA-tooling or final-prep commit can make an earlier local RC tag no longer represent current `develop` | Treat `v0.1.0-rc1`, `v0.1.0-rc2`, and `v0.1.0-rc3` as local historical evidence after final metadata prep advances `develop` | Do not push stale RC tags after follow-up commits; use the final `v0.1.0` local tag gate for the prepared final metadata |
| RC2 tag gate bypass | A current RC2 tag could be created before version metadata, release notes, QA, or rc1 preservation are verified | Require OSW-AUTO-045 version bump, feature-branch QA, squash merge, post-merge pre-tag QA, rc1 verification, and annotated rc2 verification | Do not create or push `v0.1.0-rc2` outside the focused RC2 gate; after OSW-AUTO-047/048, use the RC3 gate if a current pushable RC is needed |
| RC3 tag gate bypass | A current RC3 tag could be created before rc1/rc2 preservation, P2 QA closure evidence, release metadata, or post-merge QA is verified | Require OSW-AUTO-049 version bump, multiple-prior-RC metadata checks, docs-link QA, duplicate-basename QA, default pytest, squash merge, post-merge pre-tag QA, and annotated rc3 verification | Do not create or push `v0.1.0-rc3` outside the focused RC3 gate; keep final `v0.1.0` and public push in separate gates |
| Public tag push without approval | A local release-candidate or historical tag could be treated as a public release before maintainer approval | Keep local tag creation separate from push approval and require explicit maintainer instruction before any tag push. OSW-AUTO-077 was limited to the approved final `v0.1.2` tag and did not run `git push --tags`. | Stop and keep any unapproved tag unpushed until maintainer direction is explicit |
| Final tag gate bypass | The final `v0.1.0` tag could be created during metadata prep or review instead of the final tag gate | Keep final tag creation blocked until OSW-AUTO-054 verifies final metadata, prior RC preservation, final-tag absence, and local QA | Do not create or move `v0.1.0` in RC or final-prep prompts |
| Third-party notices require maintainer review | Optional dependencies and plugin ecosystems may have redistribution obligations that are not fully captured in source docs | Maintain `docs/14_third_party_notices.md`, review notices before source/wheel release, and perform separate review before any binary distribution | Ship source-first artifacts and avoid bundled external solver binaries |
| External solver binaries accidentally bundled | Release artifacts could inherit unreviewed redistribution duties or platform-specific binary risk | Keep CalculiX, OpenFOAM, Gmsh, GNU Octave, and SU2 as user-installed optional runtime tools | Remove binaries from artifacts and document install-only handoff |
| Release artifacts staged by mistake | `dist/`, `build/`, `wheelhouse/`, logs, or generated solver outputs could enter a release commit | Run solver artifact and release metadata scans before merge and before any tag prompt | Stop release, remove artifacts from the index, and rerun QA before tagging |
| Broad `pytest -q` collection mismatch | A single all-tests command can fail before running tests if duplicate test basenames reappear | OSW-AUTO-047 fixed the known mismatch; keep `tools/qa/check_duplicate_test_basenames.py` in fast QA and final release checks | Block future duplicate basenames before merge; use `pytest -q --import-mode=importlib` only as diagnostic evidence, not as the default fix |
| Docs link checker regression | Broken local Markdown links could enter release docs or examples | OSW-AUTO-048 added the local-only checker; run `tools/qa/check_docs_links.py` in fast/pre-merge QA and final release checks; keep `.codex/reports/` excluded | Fix broken links narrowly or block release docs changes until links pass |
| External URL freshness gap | External links can change after local docs QA passes | Keep external URL freshness explicitly out of scope for the local-only docs checker and document it as a P2 follow-up if network validation is desired | Add a future opt-in network checker with maintainer approval; do not make network access part of default release QA |
| Source-install QA regression after local final tag | Fresh editable installs can expose newer-tooling failures after a local tag already exists | OSW-AUTO-057 fixed the isolated-venv ruff and pytest blockers; OSW-AUTO-058 fresh source-install retest passed with no P0/P1 blockers | Keep source-install UAT in release gates and block publication if it regresses |
| Historical `v0.1.0` tag mistaken for publishable current release | Publishing the old local final tag would omit the source-install fix now on `develop` | Preserve `v0.1.0` as local-only evidence, document that current `develop` is ahead, and select the `v0.1.1-rc1` -> `v0.1.1` patch path | Do not move or retag `v0.1.0`; run a new patch release candidate gate |
| Patch RC1 tag gate bypass | A `v0.1.1-rc1` tag could be created before metadata, historical `v0.1.0`, source-install QA, or final-tag protection checks pass | OSW-AUTO-060 required package version `0.1.1rc1`, historical tag validation, fresh isolated source-install validation, default/importlib pytest, ruff, fast/pre-merge QA, and post-merge pre-tag checks before creating local annotated `v0.1.1-rc1`; OSW-AUTO-061 verified the tag and QA evidence | Do not push `v0.1.1-rc1`; keep final `v0.1.1` blocked until a later final release gate |
| Stale release readiness wording after local tag creation | Checklist wording can continue to describe a completed local tag gate as pending, creating false P1 readiness noise | OSW-AUTO-061A marks local `v0.1.1-rc1` creation, verification, release metadata, and source-install validation as PASS while keeping final `v0.1.1` and public push blocked | If maintainers require final release to match a current RC tag exactly after this docs-only cleanup advances `develop`, run a dedicated `v0.1.1-rc2` gate |
| Patch final tag gate bypass | Final `v0.1.1` could be created during final metadata prep rather than after final-prep QA and source-install validation | OSW-AUTO-062 prepares package version `0.1.1` and final release docs only; final `v0.1.1` tag remains forbidden until OSW-AUTO-063 verifies metadata, preserved tags, and QA | Do not create or push `v0.1.1` outside the dedicated final local tag gate |
| GUI shell without end-to-end workflow glue | A credible GUI shell can still leave users relying on CLI/API fallbacks for import, prepare/generate, result/table, and report state | OSW-AUTO-067 wires GUI-native import/project-tree/property updates, bounded prepare/diagnostic workflows, table/result preview state, and report export state while preserving preview-first script safety; OSW-AUTO-068 retested this as `PASS_WITH_LIMITATIONS` with no P0/P1 blockers | Remaining GUI/manual Computer Use depth is P2; keep optional external solver live runs environment-specific; if GUI glue regresses, open a focused GUI workflow fix queue before a new public release |
| Post-`v0.1.1` product changes after parked local tag | A local final tag can be mistaken for current `develop` after follow-up fixes land | Treat `v0.1.1` as preserved local release evidence once OSW-AUTO-067 merges; OSW-AUTO-069 selects the safe `0.1.2rc1` / `v0.1.2-rc1` -> `0.1.2` / `v0.1.2` recovery path | Do not push `v0.1.1` as current after post-tag product changes; run OSW-AUTO-070 to prepare the next patch candidate instead |
| Patch `v0.1.2-rc1` tag gate bypass | The next patch RC tag could be created before version metadata, historical tag preservation, source-install validation, GUI workflow smoke, or final `v0.1.2` protection checks pass | OSW-AUTO-070 requires package version `0.1.2rc1`, repeatable historical-final release metadata checks for `v0.1.0` and `v0.1.1`, prior RC checks through `v0.1.1-rc1`, fresh source-install validation, compact GUI workflow validation, default/importlib pytest, ruff, and fast/pre-merge QA before local annotated `v0.1.2-rc1` creation | Do not create or push `v0.1.2-rc1` outside the focused RC1 gate; keep final `v0.1.2` blocked until a later final gate |
| Patch `v0.1.2` final tag gate bypass | The final patch tag could be created during final metadata prep rather than after final-prep QA, source-install validation, GUI workflow validation, and tag preservation checks | OSW-AUTO-072 prepared package version `0.1.2` and release docs only; OSW-AUTO-073 created annotated `v0.1.2` after metadata, preserved historical tags, preserved `v0.1.2-rc1`, and clean QA; OSW-AUTO-077 published only the approved final tag. | Do not create or push future final tags outside their dedicated local tag and publish gates |
| Post-publish release docs drift | Local docs can continue to describe `v0.1.2` as local-only or blocked after the GitHub source publish | OSW-AUTO-078 verifies remote `develop` and `v0.1.2`, then aligns README, CHANGELOG, checklist, license/version plan, decision log, and risk register wording. | Block artifact or announcement gates until source publish status is no longer stale |
| Historical `v0.1.2` source release mistaken for current line | The published `v0.1.2` source tag can be mistaken for current `develop` after freeze, handoff, and release-line reconciliation work lands | Preserve `v0.1.2` as historical source-release evidence and align current metadata to the new patch candidate line `0.1.3rc1` | Do not move, retarget, recreate, force-update, or reuse `v0.1.2` as the current line |
| Patch `v0.1.3-rc1` tag or branch gate bypass | The current patch RC tag or branch could be pushed before post-metadata QA, staged-artifact checks, or explicit maintainer approval | OSW-RELEASE-005 created annotated `v0.1.3-rc1`; OSW-RELEASE-008 pushed only that tag; OSW-RELEASE-009 verified the remote target. Branch push, GitHub Release, artifacts, and announcements remain separate gates. | Do not move, delete, retarget, recreate, force-push, or repush `v0.1.3-rc1`; do not push `develop` without a later explicit branch push gate |
| Cantera 3.2 deprecation warning | Future Cantera behavior may change the reactor clone default and produce different educational demo behavior | Track the OSW-AUTO-066/067/068 warning as a P2 follow-up while keeping current tests passing | Add the explicit Cantera argument in a small compatibility PR if the warning becomes noisy or behavior changes |
| VFEA hallucinated geometry or loads | A drawing/image interpretation could produce a plausible but physically wrong model | Keep VFEA planning experimental, require FEASpec candidate diagnostics, validation, benchmark evidence, and human approval before export or solver handoff | Block implementation until the FEASpec IR and review flow are documented and tested |
| VFEA commercial solver dependency creep | Vision-to-FEA planning could drift into mandatory Abaqus or proprietary automation expectations | Keep the scope CalculiX-first, make Abaqus optional/non-default planning only, and run scope drift QA on VFEA docs | Remove mandatory commercial solver wording and split any exporter discussion into a later approval gate |
| VFEA overtrust in VLM output | Users may over-trust image or model output and skip engineering review | Require visible confidence, evidence, assumptions, and human-in-the-loop approval before project bridge or solver export | Keep VLM provider integration out of scope until benchmark and review requirements are accepted |
| FEASpec IR approval bypass | A future converter could treat candidate data as solver-ready before review | Keep `FEASpecCandidate` separate from approved FEASpec, require validation states and human approval before ProjectSchema or SolverAdapter handoff, and test docs/guardrails for the distinction | Block implementation gates that allow candidate-to-solver conversion without approval |
| FEASpec unit ambiguity | Image/text-derived dimensions, loads, or material values could silently use the wrong units | Require explicit units, make unknown units block approval or require human override, and centralize later conversions through UnitSystem-facing code | Keep candidate invalid until units are reviewed and documented |
| FEASpec seed overclaiming | JSON examples or benchmark seeds could be mistaken for validated solver results or VLM benchmark scores | Keep examples docs explicit that fixtures are text/JSON only, no generated solver outputs, no VLM runs, and no certification; test for prohibited claims | Rename or rewrite fixtures that imply execution, accuracy, or provider scoring before implementation gates use them |
| FEASpec model overreach | The Python model layer could drift into a production/full physics validator, full ProjectSchema persistence, solver exporter, provider API, or solver execution path | Keep `src/osw/experimental/feaspec/` limited to dataclass models, JSON I/O, structural checks, validator reports, and bridge planning; add guardrail tests against GUI, solver, VLM, credential, and command-execution imports | Split full persistence, ProjectSchema extension, CalculiX planning, and solver export into later explicit gates before any implementation |
| FEASpec validator design bypass | A future implementation could skip documented diagnostic severities, human-review gates, or solver handoff blockers and treat candidates as approved | Keep the validator contract design-only, add doc tests for required diagnostic codes and approval rules, and require a separate implementation gate before runtime validation exists | Block any converter or exporter gate that does not first satisfy the validator contract |
| FEASpec validator overtrust | Users or later code could treat field-level diagnostics as a full physics validator or solver approval | Keep validator reports explicit about blockers, human review, CalculiX field-level compatibility, and non-goals; test that candidates are not solver-ready and that no subprocess/network/solver modules are imported | Split ProjectSchema bridge, CalculiX case planning, mesh generation, and numerical validation into separate gated prompts |
| FEASpec bridge overreach | The bridge plan layer could silently drop FEASpec evidence, invent ProjectSchema defaults, mutate ProjectSchema too early, or cross into solver export | Keep approved-FEASpec preconditions, validator-report checks, bridge diagnostics, unmapped-field reporting, provenance preservation, and no-import guardrails tested | Block future bridge gates that bypass approval, hide unmapped fields, mutate ProjectSchema without a separate gate, call VLM APIs, call solver adapters/exporters, or execute solvers |
| FEASpec CalculiX planning overreach | A design-only case-plan contract could be mistaken for a CalculiX exporter, `.inp` writer, live `ccx` validation, or solver-run permission | Keep `FC_*` diagnostics, explicit mesh requirements, issue `#8` separation, and no-export/no-execution wording tested | Split case-plan model, `.inp` writer design, exporter implementation, and installed-only run validation into separate gates |
| FEASpec CalculiX case-plan model overreach | A planning object could be mistaken for a solver deck, writer handoff, ProjectSchema mutation, or permission to run `ccx` | Keep `ready_for_solver_execution` always false, require explicit mesh topology for writer readiness, map bridge/validator blockers into `FC_*` diagnostics, and test imports for no solver/VLM/command dependencies | Block any follow-up that writes `.inp` files, calls solver adapters/exporters, mutates ProjectSchema, runs `ccx`, or installs dependencies without a separate gate |
| FEASpec CalculiX INP writer design overreach | A design-only writer contract could be mistaken for an implemented `.inp` renderer, generated deck fixture, CalculiX exporter, or permission to run `ccx` | Keep `FW_*` diagnostics, writer preconditions, deterministic section ordering, golden fixture strategy, issue `#8` separation, and no-run wording tested | Split renderer implementation, golden `.inp` fixture generation, and installed-only run validation into separate gates |
| FEASpec CalculiX INP renderer overreach | A no-run text renderer could be mistaken for a CalculiX exporter, SolverAdapter integration, live `ccx` validation, physical validation, or permission to execute solvers | Keep renderer tests focused on deterministic text, blocked examples without explicit mesh topology, overwrite-guarded caller paths, no subprocess/import guardrails, and `ready_for_solver_execution=false` | Split golden fixtures, exporter packaging, and installed-only run validation into later gates; block any runner or SolverAdapter handoff without a separate approval gate |
| FEASpec CalculiX golden fixture overtrust | Static `.inp` golden text fixtures could be mistaken for solver outputs, live `ccx` validation, or proof of engineering correctness | Keep fixture README, manifest, docs, and tests explicit that fixtures are no-run renderer regression text only; keep issue `#8` separate and open until installed-only evidence exists | Remove or rewrite fixtures/docs that imply solver execution, certification, or validation; keep exporter and run gates separate |
| FEASpec CalculiX exporter overreach | A no-run export bundle could be mistaken for live `ccx` validation, SolverAdapter or runner integration, release asset generation, or permission to execute solvers | Keep exporter diagnostics, manifest, README, docs, and guardrail tests explicit that only caller-directory `.inp`, manifest, diagnostics, and README files are written, with `solver_execution_performed=false` and issue `#8` separate | Block any exporter follow-up that imports solver adapters, calls runners, invokes external commands, mutates ProjectSchema, installs dependencies, or closes issue `#8` without a separate gate |
| FEASpec CalculiX export preview overreach | A CLI preview could be mistaken for a writer command, live `ccx` validation, or permission to run a solver | Keep preview tests and docs explicit that the command writes no files, creates no directories, reports planned file names only, keeps `solver_execution_performed=false`, and leaves issue `#8` separate | Block any CLI follow-up that writes bundles, creates output directories, imports solver adapters, calls runners, invokes external commands, mutates ProjectSchema, installs dependencies, or closes issue `#8` without a separate gate |
| FEASpec CalculiX export write CLI overreach | A local no-run write command could be mistaken for live `ccx` validation, release asset generation, or permission to execute solver output | Keep CLI tests and docs explicit that writes require `--output-dir`, blocked inputs write no files, successful exports write only expected bundle files, `solver_execution_performed=false`, and issue `#8` remains separate | Block any CLI follow-up that imports solver adapters, calls runners, invokes external commands, installs dependencies, edits releases/assets, mutates ProjectSchema, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result import / run gate design overreach | A design-only post-export flow could be mistaken for implemented result import, installed-only run commands, SolverAdapter/runner integration, or live `ccx` validation | Keep the design doc and tests explicit that export, human review, installed-only run, result import, and ResultDataset/report summary are separate future gates with `FR_*` and `FI_*` diagnostics and issue `#8` still open | Block any follow-up that implements result import or run commands, invokes `ccx`, imports subprocess, calls SolverAdapter/runner code, installs dependencies, mutates ProjectSchema, edits releases/assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX installed-only run gate overreach | An explicit local run gate could be mistaken for result import, broad runner infrastructure, live issue `#8` closure, or permission to install/run arbitrary solver output | Keep dry-run as the default, require explicit execute/confirm/README acknowledgement, use installed `ccx` only, write isolated runtime metadata/logs, test fake `ccx` success/nonzero/timeout/missing cases, and keep result import, SolverAdapter, ProjectSchema mutation, release mutation, and issue closure out of scope | Block any follow-up that installs solvers, bundles `ccx`, imports results, closes issue `#8`, adds GUI direct execution, calls broad runner/SolverAdapter APIs, mutates ProjectSchema, or weakens explicit authorization checks without a separate gate |
| Live CalculiX validation skipped-missing overclaim | A missing-`ccx` installed-only audit could be mistaken for passing live issue `#8` validation | Record `skipped-missing` explicitly, keep issue `#8` open, and require a prepared-machine rerun before any closure gate | Block any follow-up that calls skipped evidence a pass, closes issue `#8`, installs solvers inside the validation gate, edits release assets, or claims certification |
| FEASpec CalculiX result import preview overreach | A result import preview could be mistaken for broad numerical parsing, ResultDataset persistence, live issue `#8` validation, or permission to rerun CalculiX | Keep CLI/docs/tests explicit that preview requires an explicit result directory, writes no files, exposes only an in-memory draft with `writes_files=false`, limits `.dat` parsing to bounded explicit candidates, limits `.frd` handling to block metadata/deferred references, performs no solver execution, and leaves issue `#8` open | Block any follow-up that writes ResultDataset files, broadens parser behavior without a separate gate, invokes `ccx`, calls SolverAdapter/runner paths, mutates ProjectSchema, installs dependencies, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX status scanner overreach | Text status summaries from `.sta`/`.cvg` could be mistaken for numeric convergence parsing, engineering correctness, ResultDataset persistence, or live issue `#8` validation | Keep scanner/docs/tests explicit that it only classifies status/progress text categories, preserves bounded snippets, and does not parse numeric convergence values, `.dat`/`.frd` numerical content, or units | Block any follow-up that converts numeric tokens to values, writes ResultDataset files, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, installs dependencies, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX `.dat` parser design overreach | The `.dat` subset design baseline could be mistaken for permission to broaden parser behavior, persist ResultDataset files, or validate live issue `#8` | Keep docs/tests explicit that the bounded implementation accepts only known explicit scalar/table candidates with unit context and still rejects unknown or unitless content | Block any follow-up that broadens parsing, writes ResultDataset files, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, installs dependencies, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX `.dat` section scanner overreach | Section metadata summaries could be mistaken for numeric `.dat` parsing, table extraction, unit inference, ResultDataset persistence, or live issue `#8` validation | Keep scanner/docs/tests explicit that it classifies headings, spans, section kinds, unsupported/unknown sections, bounded snippets, and counts only | Block any follow-up that extracts numeric values, extracts table rows or columns, infers units, writes ResultDataset files, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, installs dependencies, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX `.dat` minimal parser overreach | Bounded scalar/table candidates could be mistaken for free-form `.dat` parsing, `.frd` field parsing, unit inference, ResultDataset persistence, engineering validation, or live issue `#8` evidence | Keep parser/docs/tests explicit that it accepts only explicit scalar lines and small delimited tables with explicit unit context, preserves raw text/provenance, writes no files, and makes no correctness claims | Block any follow-up that parses unknown tables, infers units, reconstructs mesh/field data, writes ResultDataset files, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX `.frd` block scanner overreach | `.frd` block metadata and deferred references could be mistaken for field parsing, mesh reconstruction, ResultDataset persistence, or live issue `#8` validation | Keep docs/tests explicit that the scanner only identifies block-boundary metadata, field-reference candidates, mesh-reference candidates, unsupported diagnostics, snippets, and candidate-only ResultDataset mapping | Block any follow-up that parses field values, reconstructs mesh topology, infers units, builds visualization arrays, writes ResultDataset files, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX ResultDataset draft mapping overreach | In-memory draft mapping could be mistaken for persisted ResultDataset files, CLI persistence behavior, validated numerical results, or live issue `#8` evidence | Keep docs/tests explicit that mapping is pure in-memory, preserves existing scanner/parser candidates, writes no files, parses no additional `.frd` numerical values, reconstructs no mesh, infers no units, and makes no correctness claims | Block any follow-up that writes ResultDataset files from the mapping layer, broadens parsing, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX ResultDataset write design overreach | A persistence design could be mistaken for implemented file writes, an import-write CLI/GUI, atomic-write code, schema implementation, validated numerical results, or live issue `#8` evidence | Keep docs/tests explicit that the gate is design-only and only defines future layout, schema/versioning, path/overwrite/atomic-write policy, artifact references, validation rules, and `FDW_*` diagnostics | Block any follow-up that writes ResultDataset files, adds persistence, adds write-capable CLI/GUI behavior, parses `.frd` values, reconstructs mesh/fields, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX ResultDataset write plan overreach | The in-memory write plan could be mistaken for persisted ResultDataset files, actual atomic writes, artifact copying, CLI behavior, validated numerical results, or live issue `#8` evidence | Keep docs/tests explicit that the model only plans output directories, standard files, artifact references, diagnostics, provenance, and limitations acknowledgement while `writes_files=false` and `result_dataset_persistence=false` | Block any follow-up that writes ResultDataset files from the plan layer, creates directories, copies artifacts, adds GUI behavior, parses `.frd` values, reconstructs mesh/fields, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX ResultDataset schema payload overreach | The in-memory schema bundle could be mistaken for persisted ResultDataset files, CLI behavior, actual JSON/README writes, validated numerical results, or live issue `#8` evidence | Keep docs/tests explicit that the model only assembles ResultDataset, manifest, diagnostics, provenance, and review README payload records in memory while `writes_files=false` and `result_dataset_persistence=false` | Block any follow-up that writes ResultDataset files from the schema layer, creates directories, copies artifacts, adds GUI behavior, parses `.frd` values, reconstructs mesh/fields, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX ResultDataset writer overreach | A library-only persistence function could be mistaken for GUI write behavior, artifact-copying flow, validated numerical result parser, or live issue `#8` evidence | Keep writer/docs/tests explicit that it writes only the five standard files from an already validated plan/schema payload, copies no artifacts, contains no CLI/GUI behavior, and executes no solver | Block any follow-up that adds GUI write/import behavior, copies original solver artifacts, parses `.frd` values, reconstructs mesh/fields, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result import write CLI overreach | The review-gated write command could be mistaken for broad ResultDataset persistence, artifact copying, GUI write behavior, solver execution, or live issue `#8` evidence | Keep docs/tests explicit that the command defaults to plan-only, requires explicit write and review acknowledgements, delegates to the library writer, writes only standard review files, copies no artifacts, and executes no solver | Block any follow-up that broadens writes beyond standard files, copies artifacts, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result import write GUI design overreach | A design-only GUI workflow could be mistaken for implemented file-dialog behavior, GUI persistence, direct solver execution, or live issue `#8` evidence | Keep docs/tests explicit that the gate only defines future entry points, panels, action states, disabled reasons, file-dialog policy, acknowledgements, and CLI/GUI consistency | Block any follow-up that adds GUI source, file dialogs, hidden writes, artifact copying, invokes `ccx`, imports SolverAdapter/runner paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result write view-model overreach | A pure UI state layer could be mistaken for a GUI command, file-dialog implementation, writer invocation path, or live issue `#8` evidence | Keep docs/tests explicit that the view-model only computes panels, rows, action states, disabled reasons, acknowledgements, preview records, and lexical save-path plans | Block any follow-up that invokes the writer from the view-model, imports PySide/Qt or GUI modules into the view-model, opens file dialogs, writes ResultDataset files, copies artifacts, invokes `ccx`, imports SolverAdapter/runner/subprocess paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result write dialog overreach | A PySide6 write dialog could be mistaken for GUI persistence, CLI write behavior, direct solver execution, or live issue `#8` evidence | Keep docs/tests explicit that the dialog renders the existing write view-model, disabled write actions, acknowledgements, planned files, safety text, optional result summary, and bounded directory selection only | Block any follow-up that invokes the writer from the dialog, writes ResultDataset files, copies artifacts, invokes `ccx`, imports SolverAdapter/runner/subprocess paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result write GUI file-dialog planning overreach | A planning-only output-directory chooser contract could be mistaken for implemented QFileDialog behavior, active GUI writes, writer integration, or live issue `#8` evidence | Keep docs/tests explicit that this gate only defines future directory-selection policy, default-directory behavior, cancel no-op behavior, path validation, create-dir and overwrite acknowledgements, CLI/GUI consistency, and future mocked tests | Block any follow-up that changes GUI source, opens QFileDialog, invokes the writer, writes ResultDataset files, creates directories during selection, copies artifacts, invokes `ccx`, imports SolverAdapter/runner/subprocess paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result write GUI file-dialog implementation overreach | Directory-only output selection could be mistaken for GUI persistence, writer integration, directory creation, artifact copying, or live issue `#8` evidence | Keep docs/tests explicit that selection updates dialog-local display and save-plan analysis only | Block any follow-up that invokes the writer from selection, writes ResultDataset files, creates output directories during selection, copies artifacts, invokes `ccx`, imports SolverAdapter/runner/subprocess paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result write GUI writer-integration design overreach | A design-only writer-integration contract could be mistaken for active GUI persistence, GUI writer calls, or live issue `#8` evidence | Keep docs/tests explicit that this gate only defines future enablement, confirmation, acknowledgement, writer-call boundary, failure handling, post-write display, retry, state refresh, and mocked tests | Block any follow-up that changes GUI or view-model source, invokes the writer, writes ResultDataset files from the GUI, copies artifacts, invokes `ccx`, imports SolverAdapter/runner/subprocess paths, changes CLI or writer behavior, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result write GUI writer integration overreach | Guarded GUI persistence could be mistaken for solver validation, artifact import/copying, CLI persistence behavior changes, or live issue `#8` evidence | Keep GUI tests and docs explicit that the dialog writes only standard ResultDataset review files through the existing library writer after enabled gates and confirmation | Block any follow-up that copies original solver artifacts, runs or opens external commands, broadens CLI/library writer behavior, invokes `ccx`, imports SolverAdapter/runner/subprocess paths, mutates ProjectSchema, edits release assets, or closes issue `#8` without a separate gate |
| FEASpec CalculiX result write GUI post-write polish overreach | Clearer post-write display could be mistaken for a new writer path, shell opener, OS clipboard integration, artifact copy behavior, or live issue `#8` evidence | Keep docs/tests explicit that the polish only formats existing writer results and copy-ready display text | Block any follow-up that adds clipboard APIs, open-output shell commands, artifact copying, CLI/library writer behavior changes, solver execution, SolverAdapter/runner/subprocess paths, ProjectSchema mutation, release mutation, or issue `#8` closure without a separate gate |
| FEASpec CalculiX result write GUI closure overreach | Closing the GUI write slice could be mistaken for live `ccx` validation, issue `#8` closure, parser completion, or broader ResultDataset persistence approval | Keep closure docs/tests explicit that the scope is complete only for experimental review-file persistence and adds no runtime behavior | Block any follow-up that treats closure as permission to run solvers, close issue `#8`, broaden parsers, mutate ProjectSchema, edit releases/assets, or claim bundled solvers/certification without a separate gate |
| Post-experimental ResultDataset scope overclaim | A summary review after many FEASpec/ResultDataset gates could be mistaken for live solver validation, release readiness, new asset readiness, or issue closure approval | Keep the scope review docs/tests explicit that OSW-VALID-004 is `skipped-missing`, issues `#6`-`#11` remain open, version metadata still reports `0.1.4rc1`, and a future release-boundary decision is required | Block any follow-up that publishes assets, bumps version metadata, edits releases, closes issues, installs solvers, runs validation, or claims live CalculiX validation passed without a separate explicit gate |
| Post-experimental release-boundary decision overreach | Selecting `v0.1.5-rc1` could be mistaken for completed metadata alignment, an existing tag, release assets, or live validation readiness | Keep the decision doc/tests explicit that it is decision-only and performs no version bump, metadata alignment, tag, release edit, asset build/upload, solver execution, or issue mutation | Block metadata, tag, release, asset, or closure work until separate gates verify the selected target and preserve the public `v0.1.4-rc1` release state |
| v0.1.5-rc1 metadata alignment overreach | Aligning metadata to `0.1.5rc1` could be mistaken for a created tag, GitHub Release, release assets, or validation completion | Keep the candidate doc/tests explicit that metadata is aligned but `v0.1.5-rc1` tag/release/assets are not created, issues `#6`-`#11` remain open, and issue `#8` remains skipped-missing | Block tag creation, release edits, asset build/upload, issue mutation, solver execution, dependency installation, or live validation claims until later explicit gates |
| v0.1.5-rc1 public release body drift | Public release notes can continue to say post-public audit pending after the public download audit has passed | OSW-PLAN-009 selects a narrow release-body note update before prepared-machine validation or a new experimental line | Use a dedicated release-body update gate; do not edit releases in planning gates, and do not change assets, tags, issues, source, or validation status while correcting the stale wording |
| Post-v0.1.5-rc1 worktrack drift | After release closure, work could jump into a new experimental line or broad validation without addressing recurring GUI aggregate timeout and release-monitoring maintenance signals | OSW-PLAN-010 selects maintenance hardening for GUI aggregate timeout behavior and release-monitoring notes while keeping prepared-machine validation separate | Run the maintenance gate before new experimental work unless a prepared validation environment is explicitly available; keep release, issue, source, asset, and solver mutations in separate gates |
| GUI aggregate timeout masking failures | Treating a timed-out aggregate GUI command as a pass could hide real test failures or incomplete coverage | Require `tools/qa/run_gui_per_file_fallback.py` to enumerate every `tests/gui/test_*.py` file and pass each file before an aggregate timeout is warning-only | Block if the aggregate command fails, if fallback is partial, or if any per-file GUI command fails or has unexpected collection errors |
| Post-v0.1.5-rc1 release monitoring drift | Public release assets, release body wording, or open validation status can become stale after the release flow closes | Track [v0.1.5-rc1 post-release monitoring](release/v0_1_5_rc1_post_release_monitoring.md) checks for prerelease/not-draft state, duplicate-free assets, retained download smoke evidence, open issues `#6`-`#11`, and no bundled-solver or certification claims | Run a narrow monitoring or release-body gate; do not mutate assets, tags, issues, or source while correcting documentation |
| Prepared-machine validation still unprepared | Rerunning live optional validation on an unprepared machine can keep all targets at `skipped-missing` and delay closure evidence | OSW-VALID-005 records the `v0.1.5-rc1` skipped-missing matrix and keeps target-specific requirements explicit for Gmsh, Octave, CalculiX, OpenFOAM, CoolProp/Cantera, and PyVista/meshio | Rerun on a machine where the relevant optional solver or Python package is already installed; keep issues open until a separate closure-review gate has passing evidence |
| Optional solver manifest UX overreach | A future manifest/health UX line could be mistaken for solver installation, bundled solver support, automatic execution, validation success, or issue closure evidence | OSW-EXP-054 selects Plugin ecosystem / optional solver manifest UX as design-scoped follow-up to OSW-VALID-005 skipped-missing evidence, with no source implementation in the selection gate | Block any follow-up that installs dependencies, bundles solvers, invokes solver commands, marks `#6`-`#11` as passed, edits releases/assets, closes issues, or claims certification without separate explicit gates |
| Optional solver manifest design-to-implementation drift | A design-only manifest UX contract could be treated as implemented schema, discovery, CLI, or GUI behavior | OSW-EXP-055 limits the contract to docs/tests and splits schema model, discovery service design, CLI doctor preview, and GUI health panel design into future gates | Block any gate that edits runtime source, CLI source, GUI source, plugin code, or ProjectSchema before a prompt explicitly authorizes that implementation slice |
| Optional solver manifest schema overreach | A declarative schema/model layer could drift into discovery, optional package imports, subprocess probes, CLI behavior, GUI panels, or solver-health execution | OSW-EXP-056 keeps probes as metadata, validates built-ins structurally, and adds guardrail tests against subprocess, `shutil.which`, and optional solver package imports | Block any follow-up that executes probe commands, imports optional stacks for discovery, shells out, installs dependencies, mutates issues, edits releases/assets, or claims validation success without a separate gate |
| Optional solver discovery design overreach | A design-only service contract could be mistaken for implemented discovery, CLI doctor behavior, GUI health state, active probes, or issue closure evidence | OSW-EXP-057 keeps discovery service work docs/tests-only and separates passive metadata inspection, future presence checks, and active validation gates | Block any follow-up that calls `shutil.which`, imports optional solver packages, shells out, executes solver probes, installs dependencies, mutates issues, edits releases/assets, or claims validation success without a separate implementation or validation gate |
| Optional solver passive discovery overreach | Passive discovery could be mistaken for active validation, CLI/GUI doctor behavior, plugin loading, solver execution, or issue closure evidence | OSW-EXP-058 limits source changes to manifest-consuming passive discovery with injected resolvers, default redaction, diagnostics, and no active smoke checks | Block any follow-up that turns passive presence evidence into validation success, runs external commands, imports optional solver packages directly, installs dependencies, mutates issues, edits releases/assets, or claims `#6`-`#11` closure readiness without separate gates |
| Optional solver CLI doctor preview overreach | CLI passive reports could be mistaken for validation pass evidence, installer behavior, solver runner behavior, GUI health panel behavior, or issue closure readiness | OSW-EXP-059 limits `optional-solver-list`, `optional-solver-doctor`, and `optional-solver-explain` to built-in manifests and passive discovery reports with redacted output by default and no install/run-smoke/execute options | Block any follow-up that runs solver commands, imports optional packages for active probes, installs dependencies, exposes environment values by default, mutates issues, edits releases/assets, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver GUI health panel design overreach | A design-only GUI health panel contract could be mistaken for implemented GUI/view-model behavior, validation pass evidence, installer behavior, solver runner behavior, or issue closure readiness | OSW-EXP-060 keeps the panel as docs/tests-only and defines passive discovery display, redaction, validation history, user actions, accessibility, plugin trust labels, and view-model boundaries without source changes | Block any follow-up that changes GUI or view-model source, runs solver commands, adds install or run-smoke buttons, exposes environment values by default, mutates issues, edits releases/assets, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver GUI health view-model overreach | A pure view-model could drift into PySide/Qt widgets, passive discovery execution, solver command execution, installer behavior, clipboard/browser side effects, or issue closure readiness | OSW-EXP-061 limits source changes to UI-agnostic dataclass records and builders over supplied manifests and reports, with action states as display data only | Block any follow-up that imports PySide/Qt, invokes discovery from widgets, executes solver commands, imports optional solver packages, installs dependencies, opens shells/browsers/clipboards, mutates issues, edits releases/assets, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver GUI health panel implementation overreach | A PySide display panel could drift into discovery refresh, active validation, solver execution, installer behavior, clipboard/browser side effects, or issue closure readiness | OSW-EXP-062 limits GUI source to a read-only panel over an already-built view-model with disabled/display-only action placeholders | Block any follow-up that lets widgets invoke discovery directly, execute solver commands, import optional solver packages, install dependencies, open shells/browsers/clipboards, mutate issues, edit releases/assets, or claim `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver GUI export summary design overreach | A design-only export workflow could be mistaken for implemented file writes, file dialogs, clipboard integration, shell/browser opening, validation evidence, or issue closure readiness | OSW-EXP-063 keeps the export summary docs/tests-only and requires redaction by default, explicit save-path policy, overwrite confirmation, payload preview, pure view-model-derived payloads, and future mocked implementation tests | Block any follow-up that writes files, adds file dialogs, touches the OS clipboard, opens shells/browsers, runs discovery or solvers, installs dependencies, mutates issues, edits releases/assets, exports environment values by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver GUI export summary view-model overreach | A pure payload layer could be mistaken for active GUI export behavior, file writing, clipboard integration, shell/browser opening, validation evidence, or issue closure readiness | OSW-EXP-064 limits source changes to in-memory payload/rendering and side-effect-free save-plan analysis over an already-built health panel view-model | Block any follow-up that invokes GUI dialogs, writes export files, creates directories, touches the OS clipboard, opens shells/browsers, runs discovery or solvers, installs dependencies, mutates issues, edits releases/assets, exports environment values by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver GUI export summary implementation overreach | A redacted single-file export action could drift into clipboard sharing, opening output folders, discovery refresh, validation evidence, or issue closure readiness | OSW-EXP-065 limits GUI behavior to one explicit `.json`, `.md`, or `.txt` file write after save-plan checks and overwrite confirmation, with redaction by default | Block any follow-up that adds clipboard integration, shell/browser or open-folder actions, implicit parent creation, sidecar files, discovery refresh, solver execution, dependency installation, issue or release mutation, environment-value export by default, validation-pass claims, issue-closure claims, bundled-solver claims, or certification claims without a separate gate |
| Optional solver GUI discovery refresh design overreach | A design-only refresh workflow could be mistaken for GUI startup scanning, widget-owned discovery calls, active validation, solver execution, installer behavior, or issue closure readiness | OSW-EXP-066 keeps refresh docs/tests-only and requires explicit user action, future injected runner boundaries, passive discovery only, atomic view-model replacement, redaction, export interaction rules, and validation-gate separation | Block any follow-up that mutates GUI or view-model source without a separate implementation gate, refreshes automatically on startup, lets widgets invoke discovery directly, runs solver commands, installs dependencies, mutates issues, edits releases/assets, exposes environment values by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver GUI discovery refresh view-model overreach | A pure refresh state layer could be mistaken for worker implementation, GUI refresh wiring, passive discovery execution, active validation, solver execution, or issue closure readiness | OSW-EXP-067 limits source to request/result/status/action/apply helpers over already-supplied reports and existing health view-models, with stale-result and selection/filter handling only | Block any follow-up that imports Qt/PySide, mutates GUI source, creates workers or threads, calls passive discovery services, runs solver commands, installs dependencies, mutates issues, edits releases/assets, exposes environment values by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver GUI discovery refresh implementation overreach | Explicit GUI refresh could be mistaken for startup scanning, active validation, solver execution, dependency installation, validation evidence, or issue closure readiness | OSW-EXP-068 limits GUI behavior to an explicit user action, injected/default passive discovery runner, atomic accepted view-model replacement, failed/canceled/stale preservation, redaction defaults, and export-after-refresh consistency | Block any follow-up that refreshes automatically on startup, adds background workers without a separate gate, runs active smoke checks or solver commands, installs dependencies, mutates issues or releases, exposes environment values by default, claims `#6`-`#11` validation success, or suggests issue closure readiness |
| Optional solver plugin manifest loading design overreach | A design-only source/trust contract could be mistaken for implemented plugin loading, filesystem scanning, network marketplace behavior, trusted-by-default third-party manifests, or permission to execute plugin code | OSW-EXP-069 keeps plugin manifest loading docs/tests-only and requires data-only manifests, schema validation, visible source/trust labels, invalid-manifest quarantine, built-ins winning by default, and no network marketplace in initial scope | Block any follow-up that changes runtime, CLI, or GUI source; scans plugin directories; fetches manifests from a network; executes plugin code; trusts third-party manifests by default; runs solvers; installs dependencies; mutates issues or releases; or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver plugin manifest loader model overreach | A data-only loader could be mistaken for plugin package discovery, directory scanning, CLI/GUI preview behavior, or validation evidence | OSW-EXP-070 limits source to explicit dict/JSON manifest documents, schema validation reuse, source/trust labels, conflict diagnostics, and safety-policy diagnostics | Block any follow-up that imports plugin packages, scans directories, fetches manifests from a network, changes CLI/GUI behavior, executes plugin code, runs discovery or solvers, installs dependencies, mutates issues or releases, trusts third-party manifests by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver plugin manifest CLI preview overreach | A preview command could be mistaken for plugin package discovery, directory scanning, network fetching, trusted plugin integration, or validation evidence | OSW-EXP-071 limits CLI behavior to explicit JSON files, text/JSON preview output, built-in conflict context, strict mode, source/trust labels, and diagnostics | Block any follow-up that imports plugin packages, scans directories, fetches manifests from a network, executes plugin code, runs discovery or solvers, installs dependencies, mutates issues or releases, trusts third-party manifests by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver plugin manifest GUI design overreach | A design-only GUI contract could be mistaken for implemented file dialogs, plugin activation, directory scanning, plugin loading, or validation evidence | OSW-EXP-072 limits work to docs/tests defining future entry points, preview flow, accepted/rejected/conflict panels, diagnostics, source/trust labels, privacy, and future view-model boundaries | Block any follow-up that mutates GUI/runtime source, scans directories, loads plugin packages, fetches manifests from a network, activates manifests, runs discovery or solvers, installs dependencies, mutates issues or releases, trusts third-party manifests by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver plugin manifest GUI view-model overreach | A pure display model could be mistaken for GUI widgets, file dialogs, file loading, plugin activation, discovery with plugin manifests, or validation evidence | OSW-EXP-073 limits source to deterministic rows, trust badges, guidance text, filters, and unavailable/future action states over already-built loader reports | Block any follow-up that imports PySide/Qt, loads files, parses JSON, scans directories, loads plugin packages, fetches manifests from a network, activates manifests, runs discovery or solvers, installs dependencies, mutates issues or releases, trusts third-party manifests by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver plugin manifest GUI implementation overreach | A display-only PySide panel could be mistaken for explicit import, activation, discovery with plugin manifests, or validation evidence | OSW-EXP-074 limits GUI source to rendering an already-built plugin manifest GUI view-model with disabled/future action states | Block any follow-up that adds file dialogs, loads files from widgets, activates manifests, imports plugin packages, scans directories, fetches network manifests, runs discovery or solvers, installs dependencies, mutates issues or releases, trusts third-party manifests by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver plugin manifest explicit import GUI design overreach | A design-only explicit JSON import/preview GUI contract could be mistaken for permission to load, activate, trust, validate, install, discover, or execute plugin manifests | OSW-EXP-075 keeps the gate design-only, requires future separate implementation and activation gates, keeps user-selected and plugin-provided manifests untrusted with explicit source/trust labels, and keeps preview distinct from validation; any runtime source mutation or GUI file-loading implementation is blocked in this gate | Block any follow-up that adds file dialogs/QFileDialog, loads files, parses JSON from GUI source, activates manifests, imports plugin packages, scans directories, fetches network manifests, runs discovery or solvers, installs dependencies, mutates issues or releases, trusts third-party manifests by default, or claims `#6`-`#11` validation success or closure readiness without separate gates |
| Optional solver plugin manifest explicit import GUI view-model overreach | A pure explicit-import view-model could be mistaken for permission to load, activate, validate, install, discover, trust, or execute user/plugin manifests | OSW-EXP-076 keeps the view-model pure and side-effect-free over supplied loader reports or caller diagnostics, redacts source references, keeps user/plugin manifests untrusted by default, and keeps every unsafe action disabled/future-only; file dialog, loading, activation, discovery, validation, and issue/release actions require separate future gates | Block any follow-up that adds runtime file IO, PySide/Qt import, GUI widget source, QFileDialog, JSON parsing from paths, plugin activation, plugin package import, directory scan, network fetch, discovery execution, solver execution, dependency installation, issue mutation, release mutation, validation-pass claim, issue-closure claim, bundled-solver claim, or certification claim without a separate gate |
| Optional solver plugin manifest explicit import GUI implementation overreach | The explicit import GUI could be mistaken for permission to trust, activate, validate, install, discover, or execute user/plugin manifests | Keep the GUI preview-only; require separate future gates for activation, discovery, validation, and issue/release actions; keep action states disabled/future-only; use injected file chooser/loader seams in tests; keep user/plugin manifests untrusted by default | Block any plugin activation, plugin package import, directory scan, network fetch, discovery execution, validation-pass claim, solver execution, dependency installation, issue mutation, release mutation, tag mutation, asset mutation, bundled-solver claim, or certification claim |
| Optional solver plugin manifest activation design overreach | A design-only activation contract could be mistaken for permission to trust, validate, install, discover, execute, or close issues based on user/plugin manifests | OSW-EXP-078 keeps the gate design-only, requires future separate activation view-model and GUI implementation gates, requires explicit acknowledgements, keeps trust labels distinct from certification, and keeps activation separate from validation, discovery, install, and execution | Block any runtime activation behavior, plugin package import, directory scan, network fetch, discovery execution, validation-pass claim, solver execution, dependency installation, issue mutation, release mutation, tag mutation, asset mutation, version bump, bundled-solver claim, or certification claim |
| Optional solver plugin manifest activation view-model overreach | A pure activation view-model could be mistaken for activation authorization, validation evidence, discovery execution, dependency installation, solver execution, or issue closure | OSW-EXP-079 keeps the view-model pure and side-effect-free over supplied preview/candidate data, supplies acknowledgement/lifecycle state without persisting it, redacts source references, keeps user/plugin manifests untrusted by default, and keeps every unsafe action disabled/future-only | Block any runtime activation persistence, GUI button behavior, plugin package import, directory scan, network fetch, discovery execution, validation-pass claim, solver execution, dependency installation, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate |
| Optional solver plugin manifest activation GUI overreach | A PySide activation panel could be mistaken for persistent activation, discovery integration, validation evidence, dependency installation, solver execution, or issue closure | OSW-EXP-080 keeps the GUI view-model driven and non-persistent, limits interaction to widget-local injected acknowledgement callbacks, keeps user/plugin manifests untrusted by default, and keeps every unsafe action disabled/future-only; persistence, discovery integration, validation, install, solver execution, deactivation, and issue/release actions require separate future gates | Block any activation persistence, plugin package import, directory scan, network fetch, discovery execution, validation-pass claim, solver execution, dependency installation, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim |
| Optional solver plugin manifest deactivation design overreach | A design-only deactivation contract could be mistaken for permission to delete manifest files, uninstall dependencies, alter discovery inputs, erase validation evidence, close issues, or mutate release state | OSW-EXP-081 keeps the gate design-only, requires future separate gates for deactivation view-model extension, GUI implementation, persistence, discovery integration, validation, install/uninstall, solver execution, and issue/release actions, and preserves provenance, redaction, and historical evidence | Block any runtime deactivation behavior, deactivation persistence, GUI button behavior, CLI behavior, file deletion, dependency uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim |
| Optional solver plugin manifest discovery-refresh integration design overreach | A design-only integration contract could be mistaken for permission to run discovery, trust third-party manifests, install dependencies, execute solvers, validate optional stacks, close issues, or mutate releases | OSW-EXP-082 keeps the gate design-only, requires future separate gates for the refresh view-model, GUI/source integration, live optional validation, install behavior, solver execution, and issue/release actions, keeps built-ins authoritative, keeps activated candidates untrusted and non-validating, and keeps deactivated candidates excluded or inactive | Block any runtime discovery integration, activation/deactivation persistence, plugin package import, directory scan, network fetch, discovery execution, validation-pass claim, solver execution, dependency installation, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim |
| Optional solver plugin manifest discovery-refresh view-model overreach | A pure refresh state/record layer could be mistaken for runtime discovery integration, passive discovery behavior change, a PySide/Qt GUI, a CLI command, trusted third-party manifests, or permission to run discovery, validate, install, or execute solvers | OSW-EXP-083 keeps the layer pure and side-effect-free, computes refresh modes, state, readiness, source inclusion/exclusion, acknowledgements, diagnostics, conflicts, unsafe-claim rows, trust badges, and action states over supplied candidate/source state only, redacts source references, keeps honesty flags false, keeps built-ins authoritative, keeps activated candidates untrusted and non-validating, and keeps deactivated candidates excluded or inactive | Block any follow-up that adds runtime discovery integration, passive discovery behavior change, GUI/CLI behavior, PySide/Qt or GUI imports, activation/deactivation persistence, plugin package import, directory scan, network fetch, discovery execution, validation-pass claim, solver execution, dependency installation, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate |
| Optional solver plugin manifest discovery-refresh GUI overreach | A view-model-driven PySide review panel could be mistaken for runtime discovery integration, validation evidence, trusted plugin integration, dependency installation, solver execution, or issue closure | OSW-EXP-084 keeps the GUI view-model driven and non-executing, renders summary, refresh mode/state, source inclusion/exclusion, deactivated candidates, acknowledgements, diagnostics, conflicts, unsafe claims, trust badges, and disabled/future actions only, keeps acknowledgement interaction widget-local and non-persistent, keeps built-ins authoritative, keeps activated candidates untrusted and non-validating, and keeps unsafe actions disabled/future-only; runtime discovery-refresh source integration, live optional validation, install behavior, solver execution, and issue/release actions require separate future gates | Block any follow-up that adds runtime discovery integration, passive discovery behavior change, activation/deactivation persistence, plugin package import, directory scan, network fetch, discovery execution, validation-pass claim, solver execution, dependency installation, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate |
| Optional solver plugin manifest deactivation view-model overreach | A pure deactivation state/record layer could be mistaken for permission to delete manifest files, uninstall dependencies or solvers, alter discovery inputs, erase validation evidence, close issues, mutate releases, or persist deactivation state | OSW-EXP-085 keeps the view-model pure and side-effect-free, computes deactivation readiness, acknowledgements, diagnostics, shared-stack warnings, evidence retention, trust badges, and action states over supplied candidate state only, redacts source references, keeps honesty flags false, keeps built-ins authoritative, keeps user/plugin candidates untrusted and non-validating, retains historical evidence, and keeps unsafe actions disabled/future-only; persistence, GUI deactivation, CLI behavior, source/discovery integration, validation, install/uninstall, solver execution, and issue/release actions require separate future gates | Block any follow-up that adds runtime deactivation behavior, deactivation persistence, file deletion, dependency uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate |
| Optional solver plugin manifest deactivation GUI overreach | A view-model-driven PySide review panel could be mistaken for permission to delete manifest files, uninstall dependencies or solvers, alter discovery inputs, erase validation evidence, close issues, mutate releases, or persist deactivation state | OSW-EXP-086 keeps the GUI view-model driven and non-mutating, renders summary, candidates, acknowledgements, diagnostics, shared-stack warnings, evidence retention, trust badges, and disabled/future actions only, keeps acknowledgement interaction widget-local and non-persistent, keeps built-ins authoritative, keeps user/plugin candidates untrusted and non-validating, retains historical evidence, and keeps unsafe actions disabled/future-only; persistence, source/discovery integration, CLI behavior, validation, install/uninstall, solver execution, and issue/release actions require separate future gates | Block any follow-up that adds runtime deactivation behavior, deactivation persistence, file deletion, dependency uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate |
| Optional solver plugin manifest reactivation design overreach | A design-only reactivation contract could be mistaken for permission to trust a previously deactivated manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, erase deactivation history, close issues, or mutate release state | OSW-EXP-087 keeps the gate design-only, requires future separate gates for the reactivation view-model extension, GUI implementation, persistence, discovery integration, validation, install/uninstall, solver execution, and issue/release actions, keeps reactivation explicit/acknowledged/non-automatic, keeps built-ins authoritative, keeps user/plugin candidates untrusted and non-validating, requires re-preview for stale sources, and preserves provenance, redaction, deactivation history, and historical evidence | Block any runtime reactivation behavior, reactivation persistence, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim |
| Optional solver plugin manifest reactivation view-model overreach | A pure reactivation state/record layer could be mistaken for permission to trust a previously deactivated manifest, automatically activate it, restore/rewrite files, install dependencies, execute solvers, alter discovery inputs, erase deactivation history, close issues, mutate releases, or persist reactivation state | OSW-EXP-088 keeps the view-model pure and side-effect-free, computes reactivation readiness, acknowledgements, diagnostics, shared-stack warnings, stale-source/re-preview rows, deactivation-history/evidence retention, trust badges, and action states over supplied candidate state only, redacts source references, keeps honesty flags false, keeps built-ins authoritative, keeps user/plugin candidates untrusted and non-validating, requires re-preview for stale sources, routes back through future activation review, and retains deactivation history and historical evidence; persistence, GUI reactivation, CLI behavior, source/discovery integration, validation, install/uninstall, solver execution, and issue/release actions require separate future gates | Block any follow-up that adds runtime reactivation behavior, reactivation persistence, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate |
| Optional solver plugin manifest reactivation GUI overreach | A view-model-driven PySide review panel could be mistaken for permission to trust a previously deactivated manifest, automatically activate it, restore/rewrite/delete files, install/uninstall dependencies, execute solvers, alter discovery inputs, erase deactivation history, close issues, mutate releases, or persist reactivation state | OSW-EXP-089 keeps the GUI view-model driven and non-mutating, renders summary, candidates, acknowledgements, diagnostics, shared-stack warnings, stale-source/re-preview rows, deactivation-history/evidence retention, trust badges, and disabled/future actions only, keeps acknowledgement interaction widget-local and non-persistent, keeps built-ins authoritative, keeps user/plugin candidates untrusted and non-validating, requires re-preview for stale sources, routes back through future activation review, retains deactivation history and historical evidence, and keeps unsafe actions disabled/future-only; persistence, source/discovery integration, CLI behavior, validation, install/uninstall, solver execution, and issue/release actions require separate future gates | Block any follow-up that adds runtime reactivation behavior, reactivation persistence, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate |
| Optional solver plugin manifest state persistence design overreach | A design-only persistence contract could be mistaken for permission to save trusted state, restore a previously deactivated manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, leak local paths, erase history, close issues, or mutate release state | OSW-EXP-090 keeps the gate design-only, requires future separate gates for the persistence view-model, schema model, GUI, CLI, export summaries, source integration, discovery integration, validation, install/uninstall, solver execution, and issue/release actions, keeps persistence explicit/versioned/redaction-first/acknowledgement-aware, keeps built-ins authoritative, keeps user/plugin candidates untrusted and non-validating, requires re-preview for stale sources, expires forward-action acknowledgements on reload, and preserves provenance, redaction, deactivation/reactivation history, and historical evidence | Block any runtime persistence behavior, settings file creation, project schema mutation, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim |
| Optional solver plugin manifest persistence view-model overreach | The persistence view-model could be mistaken for permission to write state files, create settings files, mutate ProjectSchema, trust a manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, erase history, close issues, mutate releases, or claim validation evidence. | Keep the view-model pure and side-effect-free; require future separate gates for file persistence, schema model, GUI persistence, CLI persistence, export summaries, source integration, discovery integration, validation, install/uninstall, solver execution, and issue/release actions; preserve provenance, redaction, deactivation/reactivation history, and historical evidence. | Block any runtime persistence behavior, file write, settings file creation, ProjectSchema mutation, GUI/CLI persistence, reload behavior, export behavior, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim. |
| Optional solver plugin manifest state export-summary design overreach | A design-only export-summary contract could be mistaken for permission to write export files, persist state, create reloadable bundles, expose local paths, trust a manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, erase history, close issues, or mutate release state | OSW-EXP-091 keeps the gate design-only, requires future separate gates for the export-summary view-model, GUI, CLI, report integration, persistence, state bundles, source integration, discovery integration, validation, install/uninstall, solver execution, and issue/release actions, keeps export summaries explicit/redaction-first/acknowledgement-aware/non-reloadable-by-default, keeps built-ins authoritative, keeps user/plugin candidates untrusted and non-validating, requires re-preview for stale sources, and preserves provenance, redaction, deactivation/reactivation history, and historical evidence | Block any runtime export behavior, file write, export file creation, reloadable bundle creation, clipboard/open-folder behavior, persistence implementation, settings file creation, project schema mutation, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim |
| Optional solver plugin manifest export-summary view-model overreach | The export-summary view-model could be mistaken for permission to write export files, copy to clipboard, attach reports, create reloadable bundles, persist state, trust a manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, expose local paths, erase history, close issues, mutate releases, or claim validation evidence. | Keep the view-model pure and side-effect-free; require future separate gates for file export, clipboard, report attachment, reloadable bundles, GUI, CLI, persistence writer, settings files, ProjectSchema integration, source integration, discovery integration, validation, install/uninstall, solver execution, and issue/release actions; preserve provenance, redaction, acknowledgement state, deactivation/reactivation history, and historical evidence. | Block any file export, file write, clipboard behavior, report attachment, reloadable bundle creation, runtime persistence behavior, settings file creation, runtime state file creation, schema file creation, ProjectSchema mutation, GUI/CLI behavior, reload behavior, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim. |
| Optional solver plugin manifest export-summary GUI design overreach | The export-summary GUI design could be mistaken for permission to write export files, copy to clipboard, attach reports, create reloadable bundles, persist state, trust a manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, leak local paths, erase history, close issues, mutate releases, or claim validation evidence. | Keep this gate design-only; require future separate gates for GUI implementation, file export, clipboard, report attachment, reloadable bundles, CLI export, persistence writer, settings files, ProjectSchema integration, source integration, discovery integration, validation, install/uninstall, solver execution, and issue/release actions; preserve provenance, redaction, acknowledgement state, deactivation/reactivation history, historical evidence, and limitations. | Block any GUI implementation, file export, file write, clipboard behavior, report attachment, reloadable bundle creation, runtime persistence behavior, settings file creation, runtime state file creation, schema file creation, ProjectSchema mutation, CLI behavior, reload behavior, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim. |
| Optional solver plugin manifest export-summary GUI overreach | The export-summary GUI could be mistaken for permission to write export files, copy to clipboard, attach reports, create reloadable bundles, persist state, trust a manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, leak local paths, erase history, close issues, mutate releases, or claim validation evidence. | Keep the GUI view-model driven and non-exporting; require future separate gates for file export, clipboard, report attachment, reloadable bundles, CLI export, persistence writer, settings files, ProjectSchema integration, report integration, source integration, discovery integration, validation, install/uninstall, solver execution, and issue/release actions; preserve provenance, redaction, acknowledgement state, deactivation/reactivation history, historical evidence, and limitations. | Block any file export, file write, clipboard behavior, report attachment, reloadable bundle creation, runtime persistence behavior, settings file creation, runtime state file creation, schema file creation, ProjectSchema mutation, CLI behavior, reload behavior, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim. |
| Optional solver plugin manifest persistence schema model overreach | The in-memory persistence schema model could be mistaken for permission to write schema/state files, create settings files, mutate ProjectSchema, trust a manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, erase history, close issues, mutate releases, or claim validation evidence. | OSW-EXP-093 keeps the schema model pure and in-memory; defines versioned records, `to_mapping`/`from_mapping` conversion, and validation diagnostics only; reuses the `OSPMG_PERSISTENCE_*` vocabulary; keeps a redaction-first policy that blocks raw absolute paths; treats unsupported or missing schema versions as migration diagnostics; keeps every non-action flag false; adapts the persistence view-model without mutating it; requires future separate gates for the persistence writer, settings/runtime state files, ProjectSchema integration, GUI/CLI persistence, reload, schema migration, export summaries, source/discovery integration, validation, install/uninstall, solver execution, and issue/release actions; and preserves provenance, redaction, deactivation/reactivation history, and historical evidence. | Block any runtime persistence behavior, file write, schema file creation, settings file creation, runtime state file creation, ProjectSchema mutation, GUI/CLI persistence, reload behavior, export behavior, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate. |
| Optional solver plugin manifest persistence GUI design overreach | The persistence GUI design could be mistaken for permission to write state files, create settings files, mutate ProjectSchema, add a save dialog, reload state, trust a manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, leak local paths, erase history, close issues, mutate releases, or claim validation evidence. | OSW-EXP-094 keeps this gate design-only; requires future separate gates for GUI implementation, file persistence, settings files, runtime state files, ProjectSchema integration, migration, CLI persistence, reload behavior, export behavior, source integration, discovery integration, validation, install/uninstall, solver execution, and issue/release actions; preserves provenance, redaction, acknowledgement expiry, deactivation/reactivation history, and historical evidence. | Block any GUI implementation, runtime persistence behavior, file write, settings file creation, runtime state file creation, schema file creation, ProjectSchema mutation, file/save dialog behavior, reload behavior, export behavior, clipboard/open-folder behavior, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate. |
| Optional solver plugin manifest persistence GUI implementation overreach | A view-model/schema-model driven PySide review panel could be mistaken for permission to persist state, write settings or runtime files, add save/reload/export behavior, trust or activate manifests, install dependencies, run discovery/validation/solvers, mutate source behavior, close issues, mutate releases, or claim validation evidence. | OSW-EXP-095 keeps the panel review-only and non-writing; renders supplied persistence view-model and in-memory schema model records; keeps acknowledgement interaction widget-local and non-persistent; keeps all persistence, settings, runtime state, schema file, ProjectSchema, file/save dialog, reload, export, clipboard/open-folder, CLI, activation/trust, discovery, validation, solver, install/uninstall, issue, release, tag, asset, version, validation-claim, issue-closure, bundled-solver, and certification actions disabled or absent; and preserves provenance, redaction, acknowledgement expiry, stale-source re-preview, conflicts, unsafe claims, history, and evidence. | Block any follow-up that adds runtime persistence behavior, file writes, settings/runtime state/schema files, ProjectSchema mutation, reload/export behavior, file/save dialogs, clipboard/open-folder actions, CLI behavior, automatic activation, trust restoration, source behavior mutation, plugin package import, directory scan, network fetch, discovery execution, validation execution, solver execution, dependency install/uninstall, solver uninstall, issue/release/tag/asset mutation, version bump, validation-pass/fail claim, issue-closure claim, bundled-solver claim, or certification claim without a separate gate. |
| Optional solver plugin manifest persistence CLI design overreach | The persistence CLI design could be mistaken for permission to write state files, create settings files, mutate ProjectSchema, add save/load commands, reload state, trust a manifest, automatically activate it, install dependencies, execute solvers, alter discovery inputs, leak local paths, erase history, close issues, mutate releases, or claim validation evidence. | OSW-EXP-096 keeps this gate design-only; requires future separate gates for CLI implementation, file persistence, settings files, runtime state files, ProjectSchema integration, migration, reload behavior, export behavior, source integration, discovery integration, validation, install/uninstall, solver execution, and issue/release actions; preserves provenance, redaction, acknowledgement expiry, deactivation/reactivation history, and historical evidence. | Block any CLI implementation, runtime persistence behavior, file write, settings file creation, runtime state file creation, schema file creation, ProjectSchema mutation, save/load/reload behavior, export behavior, clipboard/open-folder behavior, automatic activation, trust restoration, file restore/rewrite/delete, dependency install/uninstall, solver uninstall, plugin package import, directory scan, network fetch, discovery execution, validation-pass/fail claim, solver execution, issue/release/tag/asset mutation, version bump, bundled-solver claim, or certification claim without a separate gate. |
| FEASpec human review record overreach | A data record for reviewer decisions could be mistaken for GUI approval, run authorization, result import, SolverAdapter/runner integration, or permission to execute solvers | Keep record docs and guardrail tests explicit that the model captures JSON review evidence only, with `solver_execution_performed=false` and the run gate separate | Block any follow-up that turns the record model into GUI approval behavior, run gate execution, result import, ProjectSchema mutation, VLM API/credentials, solver adapter/runner calls, dependency install, or issue `#8` closure without a separate gate |
| FEASpec human review CLI overreach | Record-only CLI approval commands could be mistaken for GUI approval, no-run export execution permission, installed-only run implementation, result import, or live `ccx` validation | Keep CLI docs/tests explicit that create/validate/summary write only review JSON, never write export bundles, keep `solver_execution_performed=false`, and leave issue `#8` open | Block any follow-up that adds GUI code, result import, run gate behavior, SolverAdapter/runner/subprocess calls, ProjectSchema mutation, dependency install, VLM API/credentials, or issue closure without a separate gate |
| FEASpec human review GUI dialog overreach | A design-only dialog contract could be mistaken for runtime GUI approval, result import, installed-only run behavior, or solver execution permission | Keep dialog docs/tests explicit that the gate only maps record/CLI evidence into future entry points, panels, warning acceptance, approval gating, record preview, save behavior, and view-model planning | Block any follow-up that adds GUI source, result import, run gate behavior, SolverAdapter/runner/subprocess calls, ProjectSchema mutation, dependency install, VLM API/credentials, issue closure, or solver execution without a separate gate |
| FEASpec human review dialog view-model overreach | A UI-agnostic state/action layer could be mistaken for a PySide/Qt GUI, result import, installed-only run implementation, or permission to execute solvers | Keep source guardrails and docs/tests explicit that the view-model computes panels, diagnostic rows, warning rows, action availability, record preview, and save-plan analysis only | Block any follow-up that imports PySide/Qt or GUI modules, writes review files, implements result import or run gates, calls SolverAdapter/runner/subprocess paths, mutates ProjectSchema, installs dependencies, adds VLM APIs/credentials, validates issue `#8`, or executes solvers without a separate gate |
| FEASpec human review GUI dialog implementation overreach | A read-only PySide6 dialog could be mistaken for review-record save integration, result import, installed-only run behavior, or permission to execute solvers | Keep GUI tests and source guardrails explicit that the dialog only renders existing view-model state, disabled reasons, safety copy, and record preview | Block any follow-up that adds file dialogs, record writes, result import, run gates, SolverAdapter/runner/subprocess paths, ProjectSchema mutation, dependency install, VLM APIs/credentials, issue `#8` validation, issue closure, or solver execution without a separate gate |
| FEASpec human review GUI save overreach | Explicit review-record JSON save could be mistaken for file-dialog support, export bundle generation, result import, installed-only run behavior, or solver execution permission | Keep save tests under `tmp_path`, require explicit caller paths, validate records before writing, refuse unacknowledged overwrites, and guard against `.inp` or export bundle outputs | Block any follow-up that adds file dialogs, export bundle writes, `.inp` writes, result import, run gates, SolverAdapter/runner/subprocess paths, ProjectSchema mutation, dependency install, VLM APIs/credentials, issue `#8` validation, issue closure, or solver execution without a separate gate |
| FEASpec human review file-dialog design overreach | A design-only path chooser could be mistaken for implemented `QFileDialog` behavior or a broader GUI export/run surface | Keep this gate docs/tests only, define path selection as updating the existing save plan, and require future implementation tests for cancel, unsafe filenames, overwrite prompts, no parent creation, and no solver/export side effects | Block any follow-up that mutates GUI source, writes export bundles, creates hidden parents, imports results, implements run gates, calls SolverAdapter/runner/subprocess paths, mutates ProjectSchema, installs dependencies, adds VLM APIs/credentials, validates issue `#8`, or executes solvers without a separate gate |
| FEASpec human review file-dialog implementation overreach | A review-record JSON chooser could be mistaken for an export-bundle chooser, result import flow, or run gate | Keep the implementation limited to mocked/testable `QFileDialog.getSaveFileName`, deterministic JSON filenames, overwrite confirmation, missing-parent guards, and existing save-plan updates; choosing a path writes nothing | Block any follow-up that writes export bundles, writes `.inp`, imports results, implements run gates, calls SolverAdapter/runner/subprocess paths, mutates ProjectSchema, installs dependencies, adds VLM APIs/credentials, validates issue `#8`, closes issues, or executes solvers without a separate gate |
| Release boundary metadata mismatch | The recommended next prerelease boundary can diverge from current active metadata | Record boundary decisions separately from metadata edits and require a dedicated metadata-alignment gate before any tag or release asset work; OSW-RELEASE-023 aligned metadata to `0.1.4rc1` after the `v0.1.4-rc1` decision, and OSW-RELEASE-032 aligns metadata to `0.1.5rc1` after the `v0.1.5-rc1` decision | If metadata and selected boundary drift again, stop before tag creation and run another explicit metadata alignment gate |
| v0.1.4-rc1 tag or asset gate bypass | Candidate metadata could be mistaken for a created tag, built assets, or a published GitHub Release | Keep [v0.1.4-rc1 candidate metadata alignment](release/v0_1_4_rc1_candidate.md) explicit that no tag, assets, or release exist yet | Block tag, asset, and release commands until final revalidation and dedicated tag/asset/release gates pass |
| Packaging smoke after GUI workflow fix | Source validation can pass while wheel/sdist or desktop packaging remains untested | Keep packaging out of release/version decision prompts and treat it as a P2 follow-up | Run a dedicated packaging artifact smoke gate after the next release candidate if packaging is desired |
| Tag mutation during recovery | Retagging or moving `v0.1.0` would weaken release traceability | No-retag policy: existing rc/final tags are not deleted, moved, recreated, overwritten, retargeted, or pushed during recovery prompts | If maintainers want a special retag review, stop and use a dedicated risk-review prompt before any tag operation |

## Review Trigger Risks

Any review finding in these areas should block merge until fixed:

- native commercial CAD direct import;
- Simulink or `.mlapp` support;
- full OpenFOAM UI or full solver coverage;
- industrial certification or production CAE claims;
- GUI direct subprocess solver execution;
- committed runtime artifacts, secrets, or generated junk.
