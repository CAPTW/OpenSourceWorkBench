# Reference Image

The OpenSolver Workbench main Run screen reference screenshot is now available
at:

```text
docs/ui/reference/osw_run_screen.png
```

Use the screenshot together with these documents as the visual specification:

- `docs/ui/gui_reference_spec.md`
- `docs/ui/theme_system.md`

Future visual QA can compare the implemented PySide6 shell against that
reference. The screenshot must not be used as a flat background image in the
application.
