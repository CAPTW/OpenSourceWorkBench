# v0.1.4 Next Feature Line

`v0.1.4` is the next feature development line after `v0.1.3rc2` maintenance
items are triaged. It should improve real user workflows while preserving the
preview-first safety boundaries established in v0.1.

OpenSolver Workbench remains educational/research software. It is not a stable
production or industrial-certified solver platform. Users must validate
engineering results independently.

## Purpose

- Start the next feature development cycle after maintenance triage.
- Improve onboarding and tutorial paths for source installs and portable use.
- Make project, plugin, result, field, and report workflows easier to inspect.
- Plan optional solver validation dashboards without making optional solvers
  mandatory.

## Candidate Themes

| Theme | Candidate direction | Guardrail |
| --- | --- | --- |
| Onboarding tutorials | Add task-oriented tutorials for demo project, plugin health, report summary, and result inspection. | Use existing fixtures and avoid commercial software. |
| Project workflow polish | Improve project validation messages, navigation, and common CLI/GUI handoffs. | Keep mutation previewable and explicit. |
| Plugin manager UX | Improve install receipts, rejection messages, local ZIP/folder diagnostics, and trust wording. | No remote plugin marketplace or dependency auto-install unless separately scoped. |
| ResultViewer workflow | Improve table, figure, and catalog navigation for ResultDataset objects. | Keep heavy visualization dependencies optional. |
| FieldViewer workflow | Improve field metadata display and safe rendering diagnostics. | No full solver field parser claim without explicit parser evidence. |
| Report/export quality | Improve report sections, warnings, and reproducibility notes. | Reports must not execute solvers, scripts, or hidden side effects. |
| Optional solver validation dashboard | Summarize installed/missing/pass/fail optional dependency evidence. | Dashboard is evidence display, not certification. |
| VFEA planning | Consider experimental VFEA planning only if explicitly scoped later. | Planning only; no implicit solver accuracy or production claim. |

## Non-Goals

- No MATLAB, ANSYS, or Simulink clone.
- No industrial certification or compliance claim.
- No commercial native CAD direct import.
- No unreviewed automatic execution of scripts or solvers.
- No full OpenFOAM UI or broad solver coverage.
- No mandatory heavy dependencies for base import, CLI smoke, or unit tests.

## Start Criteria

- `v0.1.3rc2` maintenance tasks are triaged.
- Release asset smoke and public-release checklists have owners or issues.
- Live optional validation targets have a documented evidence plan.
- GitHub milestones and issues exist for the selected feature themes.

## Recommended First Slice After Triage

[Remaining issue triage after release trust closure](../maintenance/v0_1_3rc2_remaining_issue_triage_after_release_trust.md)
recommends issue `#12` as the next planning gate now that onboarding examples
and release-trust strategy work are documented and closed. Issue `#15` is the
best fallback if a concrete Plugin Manager UX slice is preferred before broader
planning. Issues `#6` through `#11` remain environment-dependent live validation
work, issue `#14` should wait for ResultViewer / FieldViewer feature scope, and
issue `#17` should wait until the v0.1.4 feature scope is explicitly approved.

## Selected Planning Outcome

[v0.1.4 feature selection planning](v0_1_4_feature_selection.md) recommends a
workflow/product-polish feature line:

- First implementation candidate: issue `#15`, Plugin Manager UX and install
  receipts.
- Second implementation candidate: issue `#14`, ResultViewer / FieldViewer
  workflow polish.
- Strategic planning item: issue `#17`, VFEA experimental scope definition.
- Deferred track: issues `#6` through `#11`, live optional validation on suitable
  machines.

The next gate should be `OSW-PLAN-003_V0_1_4_SCOPE_LOCK`, where the maintainer
chooses whether `#15` or `#14` starts implementation and confirms that `#17`
remains planning-only unless separately approved.

## Scope Lock

[v0.1.4 scope lock](v0_1_4_scope_lock.md) locks the first implementation slice
to issue `#15`, Plugin Manager UX and install receipts. Issue `#14` remains the
second implementation candidate. Issue `#17` remains experimental/deferred
planning, and issues `#6` through `#11` remain environment-blocked live optional
validation.

`OSW-FEAT-001_PLUGIN_MANAGER_UX_RECEIPTS` has landed, and issue `#15` has been
closed after dedicated acceptance evidence. The next feature candidate is issue
`#14`, ResultViewer / FieldViewer workflow, via
`OSW-FEAT-003_RESULT_FIELD_VIEWER_WORKFLOW`.

## First Implementation Status

`OSW-FEAT-001_PLUGIN_MANAGER_UX_RECEIPTS` implements the first locked slice for
issue `#15`. The Plugin Manager now surfaces managed install receipts,
quarantine/rejection records, safety diagnostics, and managed-root uninstall
eligibility. The implementation remains local-only: no remote catalog/store,
network install, dependency auto-install, plugin signing, marketplace behavior,
or plugin code execution during install was added.

[Plugin Manager UX closure evidence](../maintenance/v0_1_4_plugin_manager_ux_closure.md)
records the receipt, quarantine, diagnostics, managed-root safety, CLI, docs,
and test evidence for deciding whether issue `#15` is complete.

## Planning Closure

[v0.1.4 planning issue closure evidence](../maintenance/v0_1_4_planning_issue_closure.md)
records the evidence for closing issue `#12`: feature selection completed, scope
lock completed, issue `#15` implemented and closed, issue `#14` retained as the
next feature candidate, issue `#17` deferred as experimental planning, and
issues `#6` through `#11` retained as environment-dependent live validation.

## Second Implementation Status

`OSW-FEAT-003_RESULT_FIELD_VIEWER_WORKFLOW` implements the second locked slice
for issue `#14`. The ResultViewer / FieldViewer workflow now makes catalog
summary, dataset details, plot/table/field/report handoff hints, field artifact
summaries, diagnostics, PyVista optional/fallback state, and summary-first
limitations clearer.

This slice does not add solver execution, script execution, full CalculiX FRD
contour parsing, OpenFOAM field parsing, vector glyphs, streamlines, animation,
or mandatory PyVista.

[ResultViewer / FieldViewer closure evidence](../maintenance/v0_1_4_result_field_viewer_closure.md)
records the review evidence for closing issue `#14`. After that closure, issue
`#17` VFEA experimental scope definition remains the next planning candidate
unless the maintainer requests a broader v0.1.4 remaining-scope review first.

## Remaining Scope Review

[v0.1.4 remaining scope review](v0_1_4_remaining_scope_review.md) records the
post-closure issue state after issues `#15` and `#14` landed and closed. It
selects issue `#17`, VFEA experimental scope definition, as the recommended next
planning-only slice.

Issues `#6` through `#11` remain deferred live optional validation work for
machines with the relevant solver executables or optional Python packages
installed. The review does not run those validations and does not make any
optional backend mandatory.

## VFEA Experimental Scope

[VFEA experimental scope definition](vfea_experimental_scope.md) defines issue
`#17` as a planning-only experimental plugin direction. It uses a FEASpec
candidate, validator, human review, approved FEASpec, ProjectSchema bridge, and
CalculiX-first export path as planning concepts only.

The scope forbids automatic unreviewed solver execution from image or VLM
output, mandatory Abaqus dependency, VLM API integration in this slice,
topology optimization implementation, industrial certification claims, and
native commercial CAD import.

[VFEA scope closure evidence](../maintenance/v0_1_4_vfea_scope_closure.md)
records the acceptance evidence for closing issue `#17` after the planning-only
scope document and guardrail tests landed. Follow-up VFEA work must start as a
separate design or implementation gate and must preserve the human-reviewed,
validation-gated boundaries.

## Completion Review

[v0.1.4 completion review](v0_1_4_completion_review.md) records that the planned
v0.1.4 workflow/product-polish and VFEA scope-definition line is complete:
issues `#12`, `#15`, `#14`, and `#17` are closed with evidence. This is a
planning completion decision only; it does not release `v0.1.4`, bump version
metadata, create tags, edit the GitHub Release, or implement VFEA.

Remaining open issues `#6` through `#11` stay in the `live-optional-validation`
track for machines with the relevant optional solvers or packages installed.
Next choices are FEASpec IR design, live optional validation, or separate
release planning/revalidation.

[v0.1.3rc2 vs v0.1.4-rc1 release boundary decision](../release/v0_1_3rc2_or_v0_1_4_decision.md)
recommends `v0.1.4-rc1` as the next prerelease boundary if maintainers choose
release preparation. The decision does not bump metadata, create tags, edit the
GitHub Release, or upload assets.

[v0.1.4-rc1 candidate metadata alignment](../release/v0_1_4_rc1_candidate.md)
aligns package and CLI metadata to `0.1.4rc1` as the next candidate. The
candidate tag, release assets, and GitHub Release remain pending separate
release gates.

## v0.1.4-rc1 Public Prerelease And Live Validation

`v0.1.4-rc1` is now a public prerelease:

- release URL: https://github.com/CAPTW/OpenSourceWorkBench/releases/tag/v0.1.4-rc1
- tag target: `f1683b441ab308fd65318ef6de3f1282549946a1`
- version: `0.1.4rc1`

The release remains a prerelease and does not claim stable production status,
industrial certification, bundled external solvers, MSI/code signing, or VFEA
implementation.

[Live optional validation matrix for v0.1.4-rc1](../validation/live_optional_validation_matrix_v0_1_4rc1.md)
records an installed-only audit for issues `#6` through `#11`. On this machine,
Gmsh, GNU Octave, CalculiX `ccx`, OpenFOAM, CoolProp/Cantera, and
PyVista/meshio were all missing, so each target was classified as
`skipped-missing`. No dependency install, solver install, release mutation,
asset upload, tag mutation, or issue closure occurred.

[Prepared environment plan for live optional validation](../validation/live_optional_validation_environment_plan.md)
defines the required prepared-machine components, future installed-only smoke
commands, pass/partial/keep-open criteria, and closure-gate boundaries for
issues `#6` through `#11`.

## FEASpec IR Design Follow-Up

[FEASpec IR design](../experimental/feaspec_ir_design.md) records the first
post-release experimental design follow-up after VFEA scope closure. It defines
`FEASpecCandidate` versus approved FEASpec, source, units, geometry graph,
materials/sections, boundary conditions, loads, dimensions, assumptions,
evidence/confidence, diagnostics, validation states, solver compatibility,
serialization examples, and future conversion boundaries.

This is design-only evidence. It does not implement FEASpec or VFEA, does not
add VLM APIs or credentials, does not execute solvers, does not make Abaqus
mandatory, and does not change the public `v0.1.4-rc1` release.

[FEASpec examples](../experimental/feaspec_examples.md) and
[FEASpec benchmark seeds](../experimental/feaspec_benchmark_seeds.md) add
canonical JSON examples and synthetic seed fixtures for future implementation
work. They demonstrate candidate, approved, and invalid diagnostic cases with
explicit units, evidence/confidence, validation states, and planned metrics.
They are not generated solver outputs and do not imply VLM benchmark scores.

[FEASpec Python models](../experimental/feaspec_python_models.md) adds the
first experimental post-release model layer for those examples and benchmark
seeds. It can load, basic-check, and serialize FEASpec documents, but it is not
a production/full physics validator, full ProjectSchema persistence,
ProjectSchema mutation, solver adapter, VLM integration, VFEA implementation,
or solver execution path.

[FEASpec validator design](../experimental/feaspec_validator_design.md) records
the future validator contract for that model layer: pipeline phases,
diagnostic schema, severity taxonomy, required diagnostic codes, approval
rules, solver handoff gates, CalculiX-first compatibility, Abaqus
optional/non-default handling, and benchmark readiness. It is design-only and
does not add production validator code, source solver behavior, VLM APIs,
credentials, full ProjectSchema persistence, ProjectSchema mutation, solver
adapters, or release mutation.

[FEASpec validator implementation](../experimental/feaspec_validator_implementation.md)
adds the first experimental semantic validator report layer after the design
gate. It exposes `validate_feaspec`, `validate_for_solver`,
`validate_benchmark_seed`, and `explain_diagnostics`, with structured
diagnostic severities, categories, codes, phase results, solver handoff flags,
CalculiX-first field-level compatibility, Abaqus optional/non-default
diagnostics, and benchmark readiness checks. It remains post-release
development on `develop` and does not implement VFEA, full ProjectSchema
persistence, ProjectSchema mutation, solver adapters/exporters, VLM APIs, mesh
generation, numerical physics validation, solver execution, release mutation,
or asset changes.

[FEASpec to ProjectSchema bridge design](../experimental/feaspec_to_projectschema_bridge_design.md)
records the post-release bridge boundary. The follow-up
[FEASpec to ProjectSchema bridge implementation](../experimental/feaspec_to_projectschema_bridge_implementation.md)
adds an experimental draft-plan layer that requires an approved FEASpec and a
validator report with no blockers, defines field mappings and bridge
diagnostics, preserves provenance/evidence and unmapped fields, and keeps full
ProjectSchema persistence, ProjectSchema mutation, SolverAdapter/export
handoff, VLM APIs, credentials, and solver execution out of scope.

[FEASpec to CalculiX case planning](../experimental/feaspec_to_calculix_case_planning.md)
records the design-only CalculiX-first boundary after bridge planning. It
defines preconditions, a future case-plan object, mesh and element strategy,
material/section, boundary/load, step/output, diagnostics, and issue `#8`
separation without adding a case generator, `.inp` writer, SolverAdapter call,
runner call, live `ccx` validation, or solver execution.

[FEASpec to CalculiX case-plan model](../experimental/feaspec_to_calculix_case_plan_model.md)
adds the bounded experimental planning object after that design gate. It maps
approved FEASpec bridge plans into serializable CalculiX planning records,
preserves diagnostics and provenance, reports `FC_MESH_REQUIRED` until explicit
mesh topology exists, and keeps writer/export/execution behavior outside this
gate.

[FEASpec to CalculiX INP writer design](../experimental/feaspec_to_calculix_inp_writer_design.md)
records the next design-only boundary. It defines the future no-run `.inp`
renderer contract, preconditions, proposed API, result objects, deterministic
file-section ordering, `FW_*` diagnostics, golden fixture strategy, and issue
`#8` separation. It does not implement a writer, generate `.inp` files, call
SolverAdapter or runner code, run `ccx`, mutate ProjectSchema, add VLM APIs, or
change the public release.

[FEASpec CalculiX INP renderer implementation](../experimental/feaspec_to_calculix_inp_renderer_implementation.md)
adds the first no-run renderer for writer-ready case plans. It can render
deterministic `.inp` text and write it to caller-provided paths with overwrite
protection, but it still does not run `ccx`, call SolverAdapter or runner code,
mutate ProjectSchema, validate issue `#8`, add VLM APIs, or change the public
release.

[FEASpec CalculiX INP golden fixtures](../experimental/feaspec_to_calculix_inp_golden_fixtures.md)
add controlled no-run renderer text fixtures under
`tests/fixtures/feaspec/calculix_golden/`.

[FEASpec CalculiX no-run exporter](../experimental/feaspec_calculix_exporter_no_run.md)
adds a caller-directory export bundle around successful renderer output. It
writes `.inp`, manifest JSON, diagnostics JSON, and `README_RUN_FIRST.txt`
files with checksum and no-run metadata, while keeping issue `#8` live
validation, SolverAdapter/runner integration, ProjectSchema mutation, VLM APIs,
and solver execution out of scope.

[FEASpec CalculiX exporter CLI preview](../experimental/feaspec_calculix_exporter_cli_preview.md)
adds `feaspec-calculix-export-preview` as a no-run diagnostic command. It reads
FEASpec JSON, reports validation, bridge, case-plan, render, planned-file, and
diagnostic status in text or JSON, supports strict blocked-preview exit code
`2`, and does not write files, create output directories, run `ccx`, call
SolverAdapter or runner code, mutate ProjectSchema, validate issue `#8`, or
change the public release.

[FEASpec CalculiX exporter CLI write no-run](../experimental/feaspec_calculix_exporter_cli_write_no_run.md)
adds `feaspec-calculix-export-write` as an explicit local bundle writer. It
requires a caller-provided output directory, blocks unsafe basenames and
unreviewed/mesh-missing examples, supports text and JSON output, and writes
only `.inp`, manifest JSON, diagnostics JSON, and README files when the no-run
exporter succeeds. It still does not run `ccx`, call SolverAdapter or runner
code, mutate ProjectSchema, validate issue `#8`, or change the public release.

[FEASpec CalculiX result import and run gate design](../experimental/feaspec_calculix_result_import_or_run_gate_design.md)
records the next design-only boundary after no-run export. It keeps human
review, installed-only run, result import, and ResultDataset/report summary as
separate future gates, defines `FR_*` and `FI_*` diagnostics, and keeps issue
`#8` as live optional validation. It does not implement result import or a run
gate, execute CalculiX, call SolverAdapter/runner/subprocess paths, mutate
ProjectSchema, or change the public release.

[FEASpec CalculiX installed-only run gate](../experimental/feaspec_calculix_run_gate_installed_only.md)
implements the run half of that design as an explicit local gate for existing
no-run export bundles. The gate defaults to dry-run, requires explicit execute,
run confirmation, README acknowledgement, installed `ccx`, timeout, and an
isolated run directory before execution, then writes runtime logs and
`run_metadata.json`. It still does not install solvers, import results, call
SolverAdapter or broad runner code, mutate ProjectSchema, validate or close
issue `#8`, or change the public release.

[FEASpec CalculiX result import model](../experimental/feaspec_calculix_result_import_model.md)
implements the result-import half as an experimental model for explicit
directories. It classifies existing run metadata, manifests, diagnostics, logs,
and CalculiX result artifacts, preserves provenance, reports `FI_*`
diagnostics, and builds an in-memory ResultDataset draft with
`writes_files=false`. It can enrich `.dat` artifacts with bounded minimal
scalar/table candidate previews when explicit units are present. It still does
not implement a free-form `.dat` parser, parse `.frd` content, write
ResultDataset files, execute CalculiX, call SolverAdapter or runner code,
mutate ProjectSchema, validate issue `#8`, or change the public release.

[FEASpec CalculiX result import CLI preview](../experimental/feaspec_calculix_result_import_cli_preview.md)
exposes that model through `feaspec-calculix-result-import-preview`. The
command requires an explicit result directory, reports text or JSON diagnostics,
supports strict exit code `2`, writes no files, reports bounded `.dat`
candidate summaries when available, executes no solver, and leaves issue `#8`
open for a separate live validation gate.

[FEASpec CalculiX result status scanner](../experimental/feaspec_calculix_result_status_scanner.md)
adds text-only `.sta` / `.cvg` status summaries to the result import path. It
classifies progress, convergence-message, warning, error, completion, failure,
informational, and unknown lines with bounded snippets and counts. It does not
parse numeric convergence values, parse `.dat`/`.frd` numerical content, write
ResultDataset files, execute solvers, mutate ProjectSchema, or validate issue
`#8`.

[FEASpec CalculiX `.dat` minimal parser design](../experimental/feaspec_calculix_result_dat_minimal_parser_design.md)
records the `.dat` parser subset before implementation. It defines accepted
header/scalar/table preview classes, rejected unknown or unitless content,
`FP_DAT_*` diagnostics, safety limits, fixture policy, ResultDataset preview
mapping, and issue `#8` separation.

[FEASpec CalculiX `.dat` metadata section scanner](../experimental/feaspec_calculix_result_dat_section_scanner.md)
adds the first `.dat` implementation slice after the design gate. It classifies
headings, spans, section kinds, unsupported/unknown sections, bounded snippets,
and counts only. It does not extract numeric values, extract table rows or
columns, infer units, write ResultDataset files, execute CalculiX, validate
issue `#8`, or mutate release state.

[FEASpec CalculiX `.dat` minimal parser](../experimental/feaspec_calculix_result_dat_parser.md)
adds the bounded parser slice after the section scanner. It parses only
explicit scalar candidates and small delimited table candidates with explicit
unit tokens or `unit_context`, preserves raw text and provenance, enriches
result-import previews in memory, and still does not implement free-form `.dat`
parsing, `.frd` numerical field parsing, unit inference, ResultDataset persistence, solver
execution, issue `#8` validation, or release mutation.

[FEASpec CalculiX `.frd` block metadata scanner](../experimental/feaspec_calculix_result_frd_block_scanner.md)
adds `.frd` metadata visibility after the design baseline. It records
block-boundary candidates, field-reference candidates, mesh-reference
candidates, unsupported-block diagnostics, `FP_FRD_*` codes, ResultDataset
candidate-only mapping, fixture policy, and issue `#8` separation. It adds no
numerical field parsing, mesh reconstruction, ResultDataset persistence, solver
execution, SolverAdapter/runner path, ProjectSchema mutation, or release
mutation.

[FEASpec CalculiX ResultDataset draft mapping](../experimental/feaspec_calculix_result_dataset_draft_mapping.md)
adds a stable in-memory draft mapping after the `.dat` and `.frd` scanner
chain. It maps artifacts, status summaries, bounded `.dat` scalar/table
candidates, deferred `.frd` references, provenance, diagnostics, and
limitations into a future ResultDataset-shaped payload while still writing no
ResultDataset files, adding no persistence, parsing no additional `.frd`
numeric field values, reconstructing no mesh, inferring no units, executing no
solver, and leaving issue `#8` open.

[FEASpec CalculiX ResultDataset write design](../experimental/feaspec_calculix_result_dataset_write_design.md)
defines the reviewed persistence contract for that draft. It is design-only and
adds output layout, schema/versioning, explicit path and overwrite policy,
atomic-write strategy, artifact reference policy, validation-before-write rules,
CLI/GUI future design, and `FDW_*` diagnostics without implementing file
writes, ResultDataset persistence, solver execution, `.frd` numerical parsing,
mesh reconstruction, or issue `#8` validation.

[FEASpec CalculiX ResultDataset write plan](../experimental/feaspec_calculix_result_dataset_write_plan.md)
adds the in-memory planning slice after that design. It validates explicit
output-directory intent, path policy, planned standard files, artifact
references, draft diagnostics, provenance, and limitations acknowledgement
while still adding no actual file writes, no ResultDataset persistence, no CLI
or GUI behavior, no solver execution, and no issue `#8` validation.

[FEASpec CalculiX ResultDataset schema payload model](../experimental/feaspec_calculix_result_dataset_schema.md)
adds the in-memory schema payload slice after the write plan. It builds
ResultDataset, manifest, diagnostics, provenance, and review README payloads
from the reviewed draft mapping and write plan while still adding no file
writes, no ResultDataset persistence, no CLI or GUI behavior, no solver
execution, and no issue `#8` validation.

[FEASpec CalculiX ResultDataset writer](../experimental/feaspec_calculix_result_dataset_writer.md)
adds the explicit library-only persistence slice for validated write-plan and
schema-payload records. It writes exactly the standard JSON, manifest,
diagnostics, provenance, and review README files under caller-reviewed output
directories, but contains no CLI/GUI behavior, copies no solver artifacts,
executes no solver, mutates no ProjectSchema, and leaves issue `#8` open.

[FEASpec CalculiX result import write CLI](../experimental/feaspec_calculix_result_import_write_cli.md)
implements the reviewed `feaspec-calculix-result-import-write` command over the
preview, draft mapping, write plan, schema payload, and library writer layers.
It defaults to plan-only review and only writes standard ResultDataset review
files after explicit `--write`, output directory, limitations acknowledgement,
and review acknowledgement. It copies no artifacts, executes no solver, changes
no release state, and leaves issue `#8` open.

[FEASpec CalculiX result import write GUI design](../experimental/feaspec_calculix_result_import_write_gui_design.md)
defines the future GUI contract for that reviewed write stack. It covers
preview entry points, panels, action states, disabled reasons, file-dialog
policy, acknowledgements, and CLI/GUI consistency without implementing GUI
source, file dialogs, CLI changes, solver execution, ProjectSchema mutation, or
issue `#8` validation.

[FEASpec CalculiX result write view-model](../experimental/feaspec_calculix_result_write_viewmodel.md)
adds the first pure Python state layer for that future GUI. It computes panels,
rows, action states, disabled reasons, acknowledgements, preview records, and
lexical save-path plans without importing PySide/Qt, implementing a GUI dialog
or file dialog, invoking the writer, writing ResultDataset files, executing
solvers, mutating ProjectSchema, or validating issue `#8`.

[FEASpec CalculiX result write dialog](../experimental/feaspec_calculix_result_write_dialog.md)
adds the PySide6 surface over that view-model. It renders source, artifact,
diagnostics, draft mapping, write plan, schema/manifest, safety, actions, and
result panels while keeping writer invocation, ResultDataset writes, solver
execution, ProjectSchema mutation, and issue `#8` validation out of scope.
Output-directory selection is handled as a later directory-only gate.

[FEASpec CalculiX result write GUI file dialog planning](../experimental/feaspec_calculix_result_write_gui_file_dialog_planning.md)
defines the future output-directory chooser contract for that display-only
dialog. It records directory-selection policy, default-directory behavior,
cancel no-op behavior, create-dir and overwrite acknowledgements, path
validation, CLI/GUI consistency, and future mocked tests while adding no
QFileDialog implementation, GUI source change, writer invocation,
ResultDataset write, solver execution, ProjectSchema mutation, or issue `#8`
validation.

[FEASpec CalculiX result write GUI file dialog](../experimental/feaspec_calculix_result_write_gui_file_dialog.md)
adds directory-only output selection to the write dialog. The chooser updates
dialog-local selected-directory display and save-plan analysis only; it invokes
no writer, writes no ResultDataset files, creates no directories during
selection, copies no artifacts, executes no solver, mutates no ProjectSchema,
and validates no issue `#8`.

[FEASpec CalculiX result write GUI writer integration design](../experimental/feaspec_calculix_result_write_gui_writer_integration_design.md)
defines the future GUI writer-integration boundary after output-directory
selection. It covers explicit enablement, final confirmation, acknowledgement
gating, single library-writer call boundary, failure handling, post-write UI,
retry behavior, state refresh, and mocked tests while adding no implementation,
no GUI file writes, no solver execution, no ProjectSchema mutation, and no
issue `#8` validation.

[FEASpec CalculiX result write GUI writer integration](../experimental/feaspec_calculix_result_write_gui_writer_integration.md)
adds the guarded GUI writer call through the existing library writer after
reviewed gates and explicit confirmation. It writes only standard
ResultDataset review files, copies no original solver artifacts, changes no
CLI/library writer behavior, executes no solver, mutates no ProjectSchema, and
validates no issue `#8`.

[FEASpec CalculiX result write GUI post-write polish](../experimental/feaspec_calculix_result_write_gui_post_write_polish.md)
improves only the result display after guarded writes. It shows written files,
hashes, diagnostics, limitations, failure details, retry guidance, and
copy-ready display text while adding no OS clipboard integration, no
open-output shell command, no artifact copying, no solver execution, no
ProjectSchema mutation, and no issue `#8` validation.

[FEASpec CalculiX result write GUI closure review](../experimental/feaspec_calculix_result_write_gui_closure_review.md)
records the closure decision for the experimental ResultDataset write GUI
slice. The scope is complete for review-file persistence, with no runtime
behavior changes, no solver execution, no live `ccx` validation, no
ProjectSchema mutation, and no issue `#8` closure.

[FEASpec human review record model](../experimental/feaspec_human_review_record_model.md)
adds the experimental data boundary for reviewed FEASpec decisions. It records
reviewer state/action, accepted-warning reasons, diagnostic decisions,
validator summary/hash, bridge/case/export summaries, acknowledgements, and
solver-execution flags as JSON-serializable evidence.

[FEASpec human review CLI approval](../experimental/feaspec_human_review_cli_approval.md)
adds record-only commands to create, validate, and summarize those JSON review
records. The CLI workflow does not implement a GUI, run gate, result importer,
ProjectSchema mutation, SolverAdapter/runner path, VLM API, dependency install,
or solver execution.

[FEASpec human review GUI dialog design](../experimental/feaspec_human_review_gui_dialog_design.md)
adds the design-only future GUI contract for entry points, panels, diagnostic
presentation, warning acceptance, approval gating, record preview, save
behavior, and CLI/GUI consistency. It does not implement GUI source, result
import, a run gate, SolverAdapter/runner/subprocess paths, ProjectSchema
mutation, VLM APIs, dependency installation, live issue `#8` validation, or
solver execution.

[FEASpec human review GUI dialog view-model](../experimental/feaspec_human_review_gui_dialog_viewmodel.md)
adds a pure Python, UI-agnostic state/action layer for that future dialog. It
computes panels, diagnostic rows, warning rows, action availability, record
preview, and save-plan analysis while keeping GUI source, PySide/Qt imports,
result import, installed-only run gates, SolverAdapter/runner/subprocess paths,
ProjectSchema mutation, VLM APIs, dependency installation, live issue `#8`
validation, and solver execution out of scope.

[FEASpec human review GUI dialog implementation](../experimental/feaspec_human_review_gui_dialog_implementation.md)
records the first read-only PySide6 dialog binding for that view-model. It
showed source evidence, diagnostics, warning rows, engineering/export
summaries, review action disabled reasons, safety copy, and record preview
only. That gate added no record save integration, no file dialog, no result
import implementation, no installed-only run gate implementation, no
SolverAdapter/runner/subprocess path, no ProjectSchema mutation, no VLM API, no
dependency install or upgrade, no issue `#8` validation, and no solver
execution.

[FEASpec human review GUI save integration](../experimental/feaspec_human_review_gui_save_integration.md)
adds explicit JSON review-record save behavior to the dialog for caller-provided
safe paths. It validates before writing, refuses unacknowledged overwrites,
does not create parent directories, and writes only one review JSON record. It
adds no file dialog, no export bundle, no `.inp`, no result import, no run gate,
no SolverAdapter/runner/subprocess path, no ProjectSchema mutation, no VLM API,
no issue `#8` validation, and no solver execution.

[FEASpec human review GUI file dialog design](../experimental/feaspec_human_review_gui_file_dialog_design.md)
records the design-only next GUI step for choosing the explicit review-record
JSON save path. It defines entry points, default filename, JSON filter,
directory, overwrite, path-safety, save-plan integration, and error-handling
rules without adding file-dialog implementation, `QFileDialog` usage, GUI source
mutation, export bundle writes, result import, run gates, SolverAdapter/runner
paths, ProjectSchema mutation, VLM APIs, issue `#8` validation, or solver
execution.

[FEASpec human review GUI file dialog implementation](../experimental/feaspec_human_review_gui_file_dialog_implementation.md)
adds the review-record JSON save-path chooser after the design gate. The chooser
uses a narrow JSON filter, deterministic sanitized default filename, cancel
no-op behavior, overwrite confirmation, parent-dir guard, and save-plan update.
It writes nothing until the existing save integration is triggered and adds no
export bundle, `.inp`, result import, run gate, SolverAdapter/runner/subprocess
path, ProjectSchema mutation, VLM API, issue `#8` validation, or solver
execution.
